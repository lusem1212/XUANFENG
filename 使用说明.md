# LUSEM 个人工作站 · 使用说明

> 单文件本地工作台:日历 + 便签 + 工地 + 客户,数据全存在本机浏览器,无需联网(除 AI 功能)。
> 最后更新:2026-08-14(V41:客户页——新增业务员字段;表单标签点击直接删除)

---

## 一、怎么打开

**双击 `LUSEM的个人工作站.html`** 即可,默认浏览器打开,离线可用。

> ⚠️ 认准主文件。旧版本在 `旧版本备份/` 子目录里,不要打开那些。
> 提示:可以在桌面上给这个文件建一个快捷方式,以后双击快捷方式更快。

---

## 二、四大页面

| 页面 | 干什么 | 怎么用 |
|------|--------|--------|
| **✦ 日历** | 记每天的安排 | 三栏布局:**左=当日计划**(计划列表+当天便签,便签完整显示且每条最前面高亮日期)、**中=日历网格**、**右=工地速览**(**施工在前、设计在后**,各自按进度排,完工沉底);添加计划=**双击日期格**打开完整浮层(时间/内容/图片/AI);**改计划=悬停条目点 ✏️**(时间和内容直接改,回车/点空白保存,Esc 取消);**勾 ✓ 完成的计划自动沉到列表底部** |
| **便签** | 收集想法/待办,打标签 | **两列瀑布流**(左右错落、卡片高度跟内容,不齐平不空荡);新建便签写内容,贴标签分类;支持图片(**点「＋内容」→「＋图片」**,拖入/粘贴/选择);**空便签缩成一条「# 日期」占位,点击展开**;可切换「全部/进行中/已完成」;**未完成便签排前、点「完成」自动沉到列尾**(取消完成自动回前面,每列各自生效);**正文=深锈棕 #8A3A12 + 14px 加粗**,红色工地标签同色系、易读 |
| **工地** | 项目控制台 | 总览分**三段独立成行**:**施工工地 / 已签设计工地 / 未签客户**(未签客户卡点击跳到客户页,卡上「转签约」可直接签约);**各段内进行中在前、完工沉底**;进看板**顶部是一行阶段胶囊**(施工 8 阶段 / 设计 6 阶段,当前高亮,点击可推进);日历右栏**工地速览**每卡显示 `3/8 · 精交底` 阶段数字 + 递进色条(已完成棕/当前橙/未完成灰);**跨阶段提醒**到达时区分**今日(红)与待办(橙)**;下方是**全量「关联记录」**——便签/日历计划只要文字提到工地名或位置名就自动同步,完整展示不截断,**未完成在前、已完成变灰划线沉底**;编辑抽屉=名称/客户/类型/阶段/标签 |
| **客户** | 客户管理(2026-08-14 新增) | **两区:未签客户(CRM 卡)+ 已签设计客户(自动聚合)**;顶栏位置=**日历/便签/客户/工地**;**新建/编辑表单=客户名/业务员/预算/楼盘地址(标签)/客户风格意向(标签)**,**联系电话已取消**(旧数据保留)**;客户卡姓名后显示业务员小字**(如 `陈女士 · 小王`);**楼盘+风格是客户专属标签**(预置选项可点选+手动输入新标签自动记住,只在本页显示,不进全站 TAGS 栏;**表单里点击标签本体=直接删除**(从候选池移除+当前客户解绑,不弹窗、不变色高亮),输入框回车=添加;卡片上悬停 × 删除客户标签);**编辑表单底部有「🗑 删除客户」按钮**(编辑时显示,新建时隐藏,确认后删除);**编辑浮层点遮罩不会关闭**(防止误触丢内容,只能点取消/×);**卡片编辑/删除按钮弱化——平时透明隐藏,鼠标悬停卡片才淡入显示**;**「✎ 转签约」按钮沉底全宽固定显示**(未签卡最底部);**已签设计客户**=从工地 type=design 按客户名自动聚合,卡片显示**客户名+设计徽标+绑定工地名+标签+注意事项**;**每张客户卡结构(从上到下):信息区(名/业务员/标签/预算)→「注意事项」(便签式内容块,＋内容,自动时间水印,可编辑/删除)→「跟进进度」(重点:每次跟进记一条,自动时间水印,最新在上倒序,可编辑/删除)→「关联记录」→「转签约」**;**来源/备注字段已取消**(旧数据自动迁移:意向需求→注意事项、跟进状态→跟进记录);客户页=记录注意事项+跟进动态的地方,工地进度去工地页看 |

---

## 三、标签系统(核心玩法)

- 输入 `#标签` 打标签,如 `#泥工 张宅防水闭水`
- **工地标签** = 工地名称(张宅/李总/王工/陈小姐),打了工地标签会自动关联该工地
- 日历/便签/工地三页顶部的 **TAGS 全站标签栏** 聚合所有标签,点击直达
- **TAGS 栏只显示进行中的工地**——工地软装完成、点看板右上「✓ 完工」后,该工地的标签会从 TAGS 栏消失(工地总览/日历速览仍可查看);点「↺ 恢复进行」可恢复
- 计划标签可直接用:日历下方标签选择器(工地组 + 事件组),点击贴入输入框
- **只输入纯标签也能保存**(如直接输 `#验收` 回车)
- **跨阶段提醒可设提醒日期**(编辑标签 → 提醒日期):填今天 = 红色「今日」提醒,留空或未来日期 = 橙色「待办」;旧提醒没设日期自动按待办显示

---

## 四、主题切换

顶栏「主题」菜单,四套主题任选,选择会记住:

- ☀ 浅色 · ☾ 深色 · ■ 粗野 · ✦ 赛博
- 左上角是 **LUSEM 品牌 LOGO**——米色挂牌上只有书法大字「家宅有度 居想室成」(放大 150%),阴影随主题适配且在深壳上清晰可见:浅/深=深投影+浅色描边光晕,粗野=直角粗黑边+**暖橙硬阴影**,赛博=直角青色描边+霓虹辉光;日历页 hero 下方 slogan 为 LOGO 英文小字「BUILD YOUR HOME · FROM YOUR LIFE」,字体用 Times New Roman(与品牌 LOGO 英文同款),琥珀金色 #E5B65C(和「今日计划」数字同色),9.6px

---

## 五、AI 功能(可选,需配 Key)

顶栏「AI」菜单填入 **DeepSeek API Key** 即可启用:

- 便签卡 ✨(卡底部最左):**AI 预判注意事项**——根据便签内容 + 关联工地的当前阶段/提醒,预判可能要注意的事和潜在冲突,卡片内显示建议列表(🔴冲突 / 🟠提醒 / ⚪注意 三种徽章);没关联工地的便签只给通用注意
- 计划输入 ✨:AI 建议标签,点一下贴入
- 工地看板:关联记录自动同步 + 阶段进度胶囊(2026-08-09 起 AI 阶段验收清单已移除)

> Key 只存在本机浏览器,不会进入备份文件。不用 AI 也完全不影响其他功能。

---

## 六、备份与恢复

顶栏「备份」菜单:

- **导出备份**:下载一个 JSON 文件(`个人工作站备份-日期.json`),包含**全部文字数据(日历/便签/工地/标签)+ 全部图片**;图片自动压缩(最长边 1200px / JPEG 75%,体积减 60~80%,肉眼无损),文件小方便传微信/GitHub
- **导入恢复**:选择导出的 JSON 恢复数据(换电脑/清缓存前必备)
- 工地类型(type 字段)会随备份一起导出/导入,旧版备份导入后自动按施工工地迁移

> 建议:定期导出一次备份文件存档,或直接复制整个「个人工作站」文件夹。

---

## 七、数据存在哪

全部存在浏览器 **localStorage**(按域名隔离,file:// 打开时即本机):

| 存储域 | 内容 |
|--------|------|
| workstation-calendar | 日历计划 |
| workstation-notes | 便签 |
| workstation-site / workstation-projects | 工地数据 |
| workstation-clients | 客户记录(楼盘 lots/风格 styles 标签、注意事项 notes、跟进进度 follows) |
| workstation-client-tags | 客户专属标签候选池(楼盘/风格预置+自定义历史,仅客户页) |
| workstation-theme | 主题选择 |
| workstation-preset-tags / workstation-tag-hidden | 标签管理 |
| workstation-ai-key / workstation-ai-stage | AI 配置与缓存 |

> 清理浏览器缓存/无痕模式会清掉数据,重要数据请先导出备份。

---

## 八、常见问题

| 问题 | 解决 |
|------|------|
| 保存没反应? | 先确认打开的是**主文件**;再确认输入了文字或标签(空内容不会保存) |
| 存了一堆图片后保存失败? | localStorage 空间满了,提示「存储空间不足」——删掉部分图片或导出备份 |
| 打开的不是最新版? | 用 `LUSEM的个人工作站.html`,旧版都在 `旧版本备份/` |
| 想找回以前的数据? | 导出备份 JSON 可导入;旧版文件打开也有各自的数据(如需要可导出迁移) |

---

*项目日志见 `项目日志.md`,制作流程见 `制作流程表.md`。*
