# Plan: 个人工作站大升级 · 新增「客户」大类

_对齐方式: grill-me 拷问 — 用户 + Hermes (2026-08-14)_

## Goal

个人工作站（单文件 HTML，v40）升级：
1. 顶栏新增第 4 个大类「客户」，页内分两区：**未签客户** + **已签设计客户**
2. 已签设计客户 ↔ 工地里 type=design 的「已签设计工地」**数据联动**（自动聚合，不重复录入）
3. 未签客户在工地页**总览第三段**新开卡片区（施工/已签设计/未签客户 三段）
4. 日历右栏「工地速览」排序：**设计排于施工之后**（各自按进度，完工沉底）
5. 「客户」大类先出 **2 个美术模板**给用户选择，选定后再执行

## Approach

1. ✅ 审查代码现状（v40：三 tab / 工地两段 / 速览混排）
2. ✅ 对齐 4 个决策点（用户已拍板，见下）
3. 📌 落盘 PLAN.md（本文件）
4. ⏳ 制作 2 个「客户」页美术模板（HTML 样张）→ 用户选版
5. ⏳ 选版后执行代码改造（分步交付）：
   - 步 A：日历速览排序（设计排施工后）——独立小改，先行交付
   - 步 B：新增 `workstation-clients` 数据表 + 未签客户 CRUD + 转签约
   - 步 C：工地页第三段「未签客户」卡片区
   - 步 D：客户页（新 tab + 已签聚合 + 未签管理）
   - 步 E：文档同步（使用说明.md / 项目日志.md）+ 旧数据回归验证

## Key decisions & tradeoffs（用户拍板记录）

| # | 决策点 | 选择 | 说明 |
|---|--------|------|------|
| 1 | 客户与工地数据关系 | **A 数据联动** | 已签客户从设计工地自动聚合（按 client 字段），不重复录入；未签客户一键转签约生成设计工地 |
| 2 | 未签客户在工地页形态 | **A 总览第三段** | 「施工工地」「已签设计工地」下方加第三段「未签客户」卡片区，风格一致、改动最小 |
| 3 | 未签客户字段 | **B 完整版(CRM式)** | 客户名 + 联系电话 + 意向需求 + 来源 + 创建日期 + 跟进状态(待跟进/已报价/谈拢中) + 预算范围 + 备注 |
| 4 | 细节确认 | **A 全部按推荐** | 转签约→生成「已签设计工地」带入客户名+备注；已签客户卡显示绑定工地名+当前进度；顶栏客户 tab 带计数；速览完工沉底 |

## Data model

- 新增 localStorage key：`workstation-clients`（数组）
  ```js
  { id, name, phone, need, source, date, status: 'pending'|'quoted'|'talking', budget, note }
  ```
- 已签设计客户 = `PROJECTS.filter(p => p.type === PROJ_TYPE_DESIGN)` 按 client 聚合（不落独立数据）
- 转签约：未签客户 → 新建 `{type:'design', name:客户名, client:客户名, note}` 工地 + 从未签列表移除（确认框提示）

## Risks / open questions

- 旧数据回归：新 key 读时容错（`load(LS_CLIENTS, [])`），无迁移
- 工地页三段布局在窄屏的响应式表现（沿用现有 site-grid 逻辑）
- 已签客户同名多工地：聚合时同 client 合并显示多个工地卡
- 未签客户「转签约」后原记录删除（先确认框）；不保留"历史未签"档案

## Out of scope

- 客户标签体系、客户与便签/日历的标签联动（本轮不做）
- 客户页不做统计报表/图表
- 不改工地看板内部逻辑
- 不引入新依赖/第三方库
