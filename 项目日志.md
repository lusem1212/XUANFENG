# 项目日志 · LUSEM 个人工作站

> 单文件 HTML 本地工作台:日历 / 便签 / 工地。
> 日志规则:每轮改版交付后追加一条;项目结束写「总结」段。
> 开始:2026-08-08

---

## 版本时间线

### v0 · 原型期(2026-08-08 上午)
- `IDEA.md` 立项(最初想法来自工地看板原型)
- 浅色版第一版:日历 + 便签两页骨架
- 备份:`index-浅色版备份.html`
- 美术样张 v1(暗壳浅心)、v2、v3(网站橙,后定稿采用)

### v1 · 暗壳浅心(2026-08-08 下午)
- 壳 #47361C / 心 #F4EEE2 视觉体系成型
- 备份:`index-v2暗壳浅心备份.html`

### v2 · 标签总线(2026-08-08 下午)
- 全站 TAGS 栏:日历/便签/工地三页标签同步关联
- 备份:`index-v3标签总线备份.html`

### v3 · 四主题(2026-08-08 傍晚)
- 粗野主义 v4.3、赛博朋克 v5.1 双路线样张 → 合并为四主题共存(浅☀/深☾/粗野■/赛博✦)
- 主题选择 localStorage 记忆
- 备份:`index-优化前备份-20260808.html`

### v4 · 正式版 + 改名(2026-08-08 18:51)
- `index.html` → `LUSEM的个人工作站.html`(正式名)
- 备份:`LUSEM 个人工作站 · 日历 _ 便签 _ 工地.html`(旧名副本,后归入 旧版本备份/)

### v5 · AI 接入(2026-08-08 18:56)
- 真 AI 版 DeepSeek 直调(file:// CORS 验证可行)
- 三入口:便签 ✨ 识别标签 / 计划 ✨ 标签注入 / 工地 📋 阶段清单(AI 生成+缓存)
- AI Key 存 localStorage,不进导出备份
- 备份:`LUSEM的个人工作站-AI接入前备份-20260808.html`

### v6 · 记录浮层(2026-08-08 19:32)
- 双击日期格打开完整记录浮层(时间/多行文本/图片/标签选择器)
- 日期格 hover ＋ 角标入口
- 备份:`LUSEM的个人工作站-双击记录浮层前备份.html`

### v7 · 工地关联闭环(2026-08-08 19:47)
- 工地标签动态派生(命中工地名/客户/位置即关联)
- 三入口闭环:打标签 → TAGS 直达 → 工地时间线聚合
- 备份:`LUSEM的个人工作站-关联闭环前备份.html`

### v8 · 标签两栏(2026-08-08 19:52)
- 日历标签选择器:工地组 + 事件组
- 备份:`LUSEM的个人工作站-标签两栏前备份.html`

### v9 · 标签增删(2026-08-08 19:59)
- 标签可增删:＋工地/＋事件内联添加,chip 悬停 × 删除
- 备份:`LUSEM的个人工作站-标签增删前备份.html`

### v10 · 草稿保存(2026-08-08 20:08)
- 输入草稿自动保存(localStorage 防抖落盘),切日期不丢内容
- 时间字段改五段式选择(早上/上午/中午/下午/晚上)
- 备份:`LUSEM的个人工作站-草稿保存前备份.html`

### v11 · 阶段一键推进(2026-08-08 20:19)
- 记录计划时若带阶段标签且晚于当前阶段 → 弹出推进按钮
- 备份:`LUSEM的个人工作站-阶段推进前备份.html`

### v12 · 便签分组(2026-08-08 20:27)
- 便签页「全部/按工地分组」视图切换 + 自定义确认框
- 备份:`LUSEM的个人工作站-便签分组前备份.html`

### v13 · 保存修复 + 目录归档(2026-08-08 20:36)
- **修复「记录计划无法保存」**:①纯 #标签被静默拦截 → 允许保存;②localStorage 脏数据导致日历面板渲染中断 → 启动自动清洗
- **目录规范化**(用户要求):`旧版本备份/` 归档 14 个历史版本、`美术样张/` 归档 5 份样张;主目录只留主文件 + 文档
- **补齐文档**:本日志 + `使用说明.md`
- 备份:`LUSEM的个人工作站-保存修复前备份-20260808.html`

### v14 · 阶段大项改版 + 完工闭环(2026-08-08 21:05)
- **施工大项重构**:拆除/水电/泥工/木工/油漆/安装/完工 → **开工 → 拆砌墙 → 精交底 → 泥工 → 木工 → 油漆 → 安装 → 软装**(8 阶段);「完工」不再是阶段,改为独立状态
- **「✓ 完工」按钮**(看板右上):软装完成后点击 → 工地标记已交付(finished);完工后该工地**从全站 TAGS 栏消失**,工地总览/日历速览仍可见;「↺ 恢复进行」可撤销
- **旧数据自动迁移**:启动时 electric→精交底、curStage=finish 的项目转 finished+软装,标签的 stage/targetStage 同步迁移;导入备份同样迁移
- 阶段色条 7→8 格(三主题新 8 色);AI 标签池补开工/拆砌墙/精交底/安装;AI 阶段清单缓存键 finish→soft
- 备份:`LUSEM的个人工作站-阶段大项改版前备份-20260808.html`

### v15 · 已签设计工地(2026-08-09)
- **工地分两类**:施工工地(8 阶段不变)+ **已签设计工地**(量房 → 确定平面 → 施工图 → 效果图 → 预算 → 签施工,6 阶段)
- **新建项目弹类型选择浮层**:🏗 施工工地 / ✏️ 已签设计工地,选中即建对应类型项目
- **总览分区块**:BUILD SITES 施工工地 / DESIGN SITES 已签设计工地,各自独立计数、进度排序;卡片与速览/看板标题带类型徽标(施工=橙 #C05A2A、设计=橄榄 #52672A,实底高对比米白字)
- **编辑项目可切换类型**:切换后当前阶段重置为该类型首阶段、清完工状态、标签阶段自动迁移(不在新类型内的归首阶段);编辑抽屉内切类型即时刷新阶段下拉
- **全链路按类型适配**:阶段一键推进(跨类型阶段词不误触)、完工/恢复(设计=「确认交付」回签施工)、AI 阶段清单(设计交付 prompt + 缓存键 d:/b: 隔离)、日历标签选择器工地 chip(设计=橄榄底)、看板底部流程提示动态切换
- **表单字段精简(2026-08-09)**:移除「位置」输入框(用户:工地名即楼盘位置,功能重复);卡片/便签区块头/右栏速览/看板标题的位置显示改为有值才显示(旧数据 loc 保留兼容,显示 `客户 · 位置` 或仅 `客户`);efSaveProj 不再写 loc(元素已删,直接读会崩)
- **⚠ 位置字段已回退恢复(2026-08-09 当天)**:用户反馈"改了没办法用了",恢复编辑抽屉位置输入框 + efSaveProj 写 loc + 各处固定显示 `客户 · 位置`;教训:用户说"不需要"≠"删除",先确认是否影响他现有数据/使用习惯再动,改前必备份(本次备份:`LUSEM的个人工作站-位置移除版备份-20260809.html`)
- 备份:`LUSEM的个人工作站-设计工地改版前备份-20260809.html`

### v16 · 备份导出 ZIP 文件夹(2026-08-09)
- **导出升级**:单个 JSON → **ZIP 文件夹**(`个人工作站备份-日期.zip`),解压后 = `backup.json`(文字数据)+ `images/`(图片独立成 .jpg/.png 文件)+ `README.txt`
- **图片独立成文件**:便签/日历计划里的图片从 base64 抽出,按 `img-001.jpg` 命名独立存放,可直接打开 / 整理进素材库;JSON 里改存 `images/xxx.jpg` 引用
- **纯手写 ZIP(store 无压缩)**:零依赖、file:// 可用;导出=CRC32+local/central/EOCD 组装,导入=解析 local header + 图片引用恢复 dataURL
- **导入双格式兼容**:新 zip(自动识别 PK 头)+ 旧版单 JSON(dataURL 内嵌)都能恢复
- **坑:central directory 字段偏移**(crc 错写 18 应为 16 等 8 处 +2 错位)→ 自解析 local header 测不出,必须用外部解压器(python zipfile)交叉验证;已修复并验证 CRC 全对、Windows 可解压
- 备份:`LUSEM的个人工作站-导出zip前备份-20260809.html`

### v17 · 便签工地主体制(2026-08-09)
- **便签以工地为主体**:新建条先选工地(自定义下拉选择器,工地名+类型色点+阶段),**不选工地不能保存**(用户拍板:方案二强制);输入框可带 #事件标签,工地名标签自动带上
- **每个工地一个大区块 + 3 列瀑布流**:区块头=类型徽标(施工橙/设计橄榄)+ 工地名 + 客户·位置 + 阶段 chip + 便签数 + 「进工地 →」;区块内便签 3 列瀑布流(窄屏降 2/1 栏),图片便签 hero 大图主视觉
- **点工地名跳转聚焦**:新建条选工地 → 滚动到该工地区块 + 橙色闪烁;点区块头 → 进工地看板;点卡片工地 chip → 聚焦
- **数据模型**:note 加 `site` 字段(工地名);`noteSiteOf(n)` 优先 site、回退 tags 工地标签推导;启动/导入时 `migrateNoteSites` 自动补 site(旧便签进底部「未归属」区,标签框输入工地名自动归位)
- **联动**:工地页新建/删除工地 → 选择器与工地区块自动增删;AI 识别出工地 → 自动归属;快捷条点工地名标签 → 直接选中新建条
- **全链路测试通过**:强制拦截/记录进区块/带图记录(hero 大图)/旧数据迁移/区块跳转/未归属区
- 备份:`LUSEM的个人工作站-便签工地主体改版前备份-20260809.html`

### v18 · 整体回退到设计工地改版前(2026-08-09)
- **用户明确要求回到 `LUSEM的个人工作站-设计工地改版前备份-20260809.html`(v14)**,主文件已整体替换为该备份
- **回退掉的 v15-v17 改动**:已签设计工地类型 / ZIP 导出(图片独立文件夹)/ 便签工地主体制(三栏瀑布流+强制选工地)/ 位置字段移除
- **恢复的旧版形态**:工地总览单一列表(无类型分区、无类型徽标)、便签「全部/按工地分组」视图+新便签按钮、备份导出 JSON(图片 base64 内嵌)、工地编辑抽屉含位置字段、新建项目直接创建(无类型浮层)
- 当前版备份:`LUSEM的个人工作站-v17版备份-20260809.html`
- **教训(重要)**:用户 8-09 连续三轮回退(位置字段→整体回退),说明一轮内叠加的"新需求改造"(设计工地+ZIP+便签主体)超出用户实际使用习惯,大改版应**分步交付、每步让用户用几天再继续**,不要一口气做完所有"听起来合理"的升级

### v19 · 从 v14 切回 v15+v16 完成态(2026-08-09)
- **用户要求"从这个(v14)改到 V15"**:主文件切换为 `LUSEM的个人工作站-便签工地主体改版前备份-20260809.html`(即 v16 完成态 = v15 已签设计工地 + v16 ZIP 导出;纯 v15 的「导出zip前备份」当时未归档,已无法精确还原)
- **当前版本功能**:工地分两类(施工工地/已签设计工地,6 阶段:量房→确定平面→施工图→效果图→预算→签施工)、新建项目类型浮层、总览双区块、编辑抽屉含位置字段、备份导出 ZIP(图片独立文件夹)、便签保持旧版「全部/按工地分组」视图
- 回退前(v14)备份:`LUSEM的个人工作站-v14版备份-20260809.html`
- 如用户只想要纯 v15(JSON 导出),需从当前版手动移除 ZIP 导出代码(导出按钮恢复 Blob JSON + 导入恢复 readAsText)

### v20 · 用户实测 v15 日历异常,再次整体回退 V14(2026-08-09)
- **用户反馈**:v15+v16 版进入日历主页无法正常打开、后续还有多处问题;明确要求 V14 替换主版本,并**在 V14 基础上重新修改**(重做设计工地功能)
- 主文件已替换为 `设计工地改版前备份`(V14);回退前备份:`LUSEM的个人工作站-v15v16版备份-20260809.html`
- **本次重做设计工地的纪律(用户+日志共同确立)**:①每次改动前备份;②一步只做一个功能点,做好测好交付,用户用顺再继续;③浏览器全链路测试含真实数据场景(日历主流程、切换页、导入导出);④不用 heredoc 改 CRLF 文件(用 patch 工具/字节级检查);⑤任何"听起来合理"的附加升级(如 ZIP/便签主体)先问用户是否要,不主动叠加上去

```
个人工作站/
├── LUSEM的个人工作站.html   ← 当前主文件(v15)
├── 使用说明.md               ← 用户手册
├── 项目日志.md               ← 本文件
├── 制作流程表.md             ← 六段制作流程沉淀
├── IDEA.md
├── 旧版本备份/               ← v0~v14 全部历史版本(16 个)
└── 美术样张/                 ← 样张 v1~v5
```
### v21 · 在 V14 上重做设计工地(2026-08-09)
- **背景**:v15-v17 三轮叠加改版(设计工地+ZIP+便签主体)被用户整体回退到 V14;本次按纪律**一步一个功能点**重做,只做「工地分两类 + 设计 6 阶段」,日历/便签/备份导出/位置字段一概不动
- **改动清单**:
  - 数据层:新增 `STAGE_DEFS_DESIGN`(量房→确定平面→施工图→效果图→预算→签施工)、`PROJ_TYPE_BUILD/DESIGN`、`stageDefsOf/stageIndexOf/stageNameOf/firstStageOf/lastStageOf`;旧数据迁移自动补 `type='build'`
  - 新建项目:点「新建项目」弹出类型选择浮层(🏗 施工 / ✏️ 已签设计),选中即建对应类型
  - 总览分区:施工工地 / 已签设计工地 两个区块各自计数排序;卡片带类型徽标(施工橙/设计橄榄)
  - 编辑抽屉:新增项目类型下拉;切换类型时阶段下拉按目标类型即时刷新、保存时重置首阶段+清完工+标签阶段迁移
  - 看板/完工:标题带类型徽标;设计工地完工按钮=「✓ 确认交付」,确认文案按类型;阶段列按类型渲染
  - 右栏速览/便签分组/标签详情:阶段名与色条全部按类型;跨类型阶段词不误触推进
  - AI 阶段清单:设计工地用「设计交付助手」prompt,缓存键 b:/d: 隔离
- **诊断结论(回答"为什么 v15 后没法用")**:v15 的 renderCal/标签函数与 v14 逐字相同,全新环境日历正常;根因是 v15 一次性改 35+ 处、叠 ZIP+便签主体,且未在用户真实数据下回归——旧数据迁移/渲染在真实数据结构下抛错即整页中断。本次改动用「模拟 v14 旧数据注入」实测迁移,全链路零报错
- 备份:`LUSEM的个人工作站-设计工地重做前备份-20260809.html`
- **教训沉淀**:①一次只做一个功能点,做顺再继续;②改数据结构的版本必须用旧数据注入回归;③"听起来合理"的附加升级(如 ZIP 导出)先问用户

### v22 · 三处微调(2026-08-09)
- **① 已签设计工地单独一行**:`.site-section` 加 `grid-column: 1 / -1` 占满整行,施工/设计两个区块彻底分开,设计工地不再跟随施工卡片混排(浏览器截图实测)
- **② 编辑项目隐藏位置字段**:编辑抽屉移除「位置」输入框(表单剩:类型/名称/客户/阶段/标签);efSaveProj 的 loc 读取加防御(`locEl ? locEl.value.trim() : proj.loc`),旧数据保留、不崩(v15 曾因直接读已删元素崩过,本次避开)
- **③ 便签页工地快捷条自动同步**:便签「常用标签」条前新增「工地」区,自动列出**进行中**工地名(与 TAGS 栏一致);点击即贴工地标签(无便签自动新建);`updateHeader` 里补调 `renderPresetBar()`,工地新建/改名/完工后快捷条自动增删(实测:新增→出现,完工→消失)
- 实测:两类工地独立成行、编辑表单无位置字段、保存保留 loc、快捷条点贴标签,零 JS 报错
- 备份:`LUSEM的个人工作站-设计工地三改前备份-20260809.html`

### v23 · 便签事件同步到工地看板顶部(2026-08-09)
- **需求**:便签里增加的事件,全部同步到对应工地里面
- **现状发现**:便签带工地标签后**本来就会**同步进工地看板「关联记录」时间线(`siteRelatedRecords` 聚合便签+日历计划),但它原在看板**最底部**、需滚动才可见 → 用户以为没同步
- **改动**:①`#siteTimeline` HTML 从阶段列下方移到看板**顶部**(alertBanner 下、AI 清单/阶段列上方),进工地第一眼即见;②标题改为「关联记录 + 红色计数徽标」,提示文案改「便签 / 日历计划中带工地标签的记录自动同步」
- 实测:关联记录在阶段列上方(tlTop 376 < stagesTop 562)、计数=2、两条便签正确显示、零 JS 报错
- 备份:`LUSEM的个人工作站-关联记录上移前备份-20260809.html`

### v24 · 关联匹配增强:文字提到工地名即自动同步(2026-08-09)
- **用户反馈**:点开工地看不到便签/日历的关联内容 → 排查发现旧版 `siteRelatedRecords` **只匹配 #标签 精确等于工地名/客户名/位置名**,用户之前填的记录大多只写文字没打标签,所以不关联
- **改动**:匹配规则升级为双通道——①标签精确匹配(原有);②**正文包含匹配**:便签/计划的文字里提到**工地名或位置名**(独特词,≥2字)即自动关联;客户名("张总"类)太通用,仅做标签精确匹配防误判
- 实测:无标签便签"红星天铂 阳台防水闭水完成"、无标签计划"红星天铂 泥工进场对接"均自动关联,无关内容正确排除,零 JS 报错
- 备份:`LUSEM的个人工作站-关联匹配增强前备份-20260809.html`

### v25 · 看板改版:进度胶囊 + 关联记录全量 + 移除 AI 清单(2026-08-09)
- **用户拍板(先对齐后执行)**:①B 阶段进度压缩成顶部一行窄胶囊(8 阶段,当前高亮,点击可推进);②C 关联记录全量展示;③去掉 AI 阶段验收清单
- **① 阶段进度**:8 个大卡片列(252px) → 一行 `.stage-pill` 胶囊(等宽、done 淡色/current 深色高亮/未到可点击推进);stageCols 点击事件从 `.stage-head` 改为整块 `[data-role=stage-col]`(当前阶段不可点)
- **② 关联记录全量**:看板 `renderSiteTimeline` 本就是全量(无 slice);**便签分组视图** `renderNoteGroups` 去掉 `.slice(0,6)` 截断和「还有 N 条」提示(工地组+未分类都全量)
- **③ 移除 AI 验收清单**:删 HTML `#stageCheck`、`renderStageCheck()` 调用与函数、`#stageCheck` 事件监听、`aiStageChecklist` 主体(留空占位防残留引用)、三主题 `.stage-check` CSS;AI 菜单注释同步更新(便签/计划 ✨ 标签识别保留)
- 实测:8 胶囊正确(泥工 current)、点木工推进成功、关联记录 3 条长文完整显示、便签分组全量无截断、无 AI 区块、零 JS 报错
- 备份:`LUSEM的个人工作站-看板改版前备份-20260809.html`

### v26 · 日历三栏布局:当日计划移到左栏(2026-08-09)
- **用户需求(先对齐后执行)**:当日计划像工地速览一样展示,放在日历左边;保留完整信息(时间下拉/图片);原日历下方面板隐藏
- **改动**:①HTML 把 `#calDayPanel` 从日历网格下方移到新 `<aside class="cal-dayaside">`(id 不变 → 全部 JS 逻辑复用),加"当日计划"栏头+计数徽标(`#dayPanelCnt`,EMPTY/DONE 进度);②CSS 三栏 `grid-template-columns:300px 1fr 320px`,1120px 以下降两栏(左栏取消 sticky),980px 以下单栏(左栏 order:2 回落到日历下方,工地速览隐藏);③当日计划面板适配窄栏:快速输入条改纵向(时间下拉+✨+图片一行,文本输入一行),计划文本单行省略防撑破
- 实测:三栏布局截图确认、计划列表/标签/图片按钮/快速输入回车保存(时间+标签解析正确)、计数徽标 1/2 DONE、零 JS 报错
- 备份:`LUSEM的个人工作站-日历左栏前备份-20260809.html`

### v27 · 当日计划左栏精简(2026-08-09)
- **用户需求(先对齐后执行)**:①隐藏当日计划栏的标签选择器和快速添加区;②便签显示更全,最前面高亮日期(方便知道是哪一天的)
- **改动**:①renderDayPanel 移除 `.dp-add.quick`(时间下拉+✨+图片+输入)和 `#calTagPicker`(工地/事件标签 chips);添加计划改走**双击日期格→完整记录浮层**(实测打开正常,含时间/文本);清理 renderDayPanel 尾部对 newTime/newText/renderCalTagPicker 的引用;②便签区每条加 `<span class="dn-date">` 高亮日期(锈橙底白字,mono 字体),`.dn-item .t` 改 `word-break:break-word` 多行完整显示(原来单行省略号)
- 残留引用安全:renderCalTagPicker/syncTagChipState 均有 `if (!box) return` 保护,元素不存在自动跳过
- 实测:日期高亮(红橙底)、长便签完整显示(whiteSpace=normal)、标签/添加区隐藏、双击浮层打开、零 JS 报错
- 备份:`LUSEM的个人工作站-日历左栏精简前备份-20260809.html`

### v28 · 便签页改版(2026-08-09)
- **用户需求(先对齐后执行)**:①一页 4 个便签 → 2 个,显示更全;②卡片内部重排——「改于xx」置最前、标签放最上、日期随后;取消图片添加/✨AI 按钮/#标签输入框的虚线框(改无边框+悬停显示);无标签不显示标签区;③隐藏便签页顶部工地快捷条+常用标签快捷条(贴标签改在便签内 #标签 输入框,页面更整洁)
- **改动**:①`.notes-grid` 改固定 2 列(`repeat(2, minmax(0,1fr))`);②renderNotes 模板重排:done-badge → n-mts → n-tags → n-date → textarea → note-imgs → tag-input → ai-suggest → n-foot;n-mts 动态插入逻辑改到卡片顶部(原来插 n-foot);③删除:快捷条/标签云/「按工地分组」视图,HTML+CSS+JS 全清理(renderPresetBar/renderTagCloud/renderNoteGroups/noteView/viewbar 事件/标签云事件/分组视图点击死代码/全部调用点),`PRESET_TAGS` 数据体系保留——日历标签选择器「事件组」仍依赖它
- 实测:注入旧数据(无 mts/images 字段)不崩;2 列布局、改于置顶、标签输入后出现在顶部 chips、无标签卡片不显示标签区、无虚线框、零 JS 报错
- 备份:`LUSEM的个人工作站-便签2列改版前备份-20260809.html`


### v29 · 便签标签选择器 + 改于位置调整(2026-08-09)
- **用户需求(先对齐后执行)**:①点击便签里的空标签位置(标签输入框),自动弹出可添加的标签——第一列=目前所有(进行中)工地,第二列=已用过的非工地标签;②「改于」时间从卡片最顶移到**日期后面**(顺序:标签 → 日期 → 改于 → 正文),每次二次编辑便签自动记录日期时间
- **改动**:①新增标签选择器:每卡片 tag-input 后挂 `.note-tagpicker` 面板(absolute 定位在输入框上方),focusin 时 `showTagPicker` 填充内容并显示;`noteTagPickerHTML` 统计——工地列=进行中工地名(PROJECTS.filter(!finished)),历史列=便签+日历计划里用过的非工地标签(isSiteTag 排除)按使用次数排序取前 24;点击 chip 贴上标签+更新 mts+收起面板;document click 面板外收起;②renderNotes 模板 `mtsHtml` 移到 n-date 后;input 事件动态插入位置改 `card.insertBefore(mEl, card.querySelector('textarea'))`(日期后正文前)
- 实测:点击输入框弹两列面板(工地A/B 显示、完工工地C 隐藏)、点击「验收」(来自日历计划)贴上并收起、点击外部收起、编辑正文后「改于 8月9日 19:52」出现在日期后、零 JS 报错
- 备份:`LUSEM的个人工作站-标签选择器改版前备份-20260809.html`


### v30 · 便签多内容块(2026-08-09)
- **用户需求(先对齐后执行)**:一条便签里可以添加多条内容条目(清单/分段)
- **改动**:①数据模型 `note.texts`(字符串数组)+ `note.text` 同步为 `texts.join('
')`——所有旧逻辑(noteMatches/siteRelatedRecords/导入导出/AI)零改动兼容;旧便签(只有 text)渲染为 1 块;②renderNotes 模板:单 textarea 改 `noteTextBlocks(n)` 多块渲染(每块 `.note-tblock` 内含 textarea+悬停×删除钮)+ 正文区末尾「＋ 内容」按钮;③textarea CSS `flex:1 0 auto` + `overflow:hidden`,input 事件里 scrollHeight 自动增高(写多少字卡片长多高);④keydown 去掉「Enter=blur」拦截(多块下回车=块内换行);⑤click 处理器加「内容块删除」「添加内容块」分支,添加后自动聚焦新块
- 实测:旧数据(仅 text)→ 1 块;新数据(texts)→ 3 块;点「＋内容」加块、×删块、长文输入自动增高 130px、texts/text 双写一致、零 JS 报错
- 备份:`LUSEM的个人工作站-便签多内容块前备份-20260809.html`


### v31 · 内容块日期 + 完成按钮高亮 + 标签字体分级(2026-08-09)
- **用户需求(先对齐后执行)**:①「＋ 内容」按钮虚线框,每个内容块标注独立日期;②「标完成」改「完成」且填充高亮;③标签字体分级(工地标签最大),日期+改于同级同一条线同色(金黄)
- **改动**:①数据 `note.textDates`(与 texts 并行数组)——添加块时自动记当天日期,删除块同步 splice;无 textDates 旧数据第一块回退用 n.date;渲染 `.tblock-date` 金色小字标块上方;②「＋内容」按钮 `border:1px dashed`(hover 实线);③「标完成」→「完成」,`.n-done-btn` 默认橄榄绿填充白字(hover 提亮);④标签分级:`.note-chip.site` 13.5px+加大 padding(普通 11px);⑤日期行:`.n-meta` flex 容器合并 n-date+n-mts,同字号 11px、同色 #C9A227 金黄,去 mts 徽标底色
- 实测:旧数据(仅 text)→ 第一块标便签日期 8月7日;点「＋内容」→ 新块标当天 8月9日;日期/改于同色 rgb(201,162,39);工地标签 13.5px;完成按钮 olive 填充;删除块日期同步;零 JS 报错
- 备份:`LUSEM的个人工作站-内容块日期前备份-20260809.html`
- **v31 补充修复(同日)**:内容块日期补上**具体时间**(如「8月9日 20:26」,textDates 存 `M月D日 HH:MM`);**第一块不再显示块日期**(便签顶部 .n-meta 日期已代表,消除"第一排两个日期"重复);第二块起显示完整日期+时间;旧数据纯日期字符串兼容显示



### v32 · 日历两栏统一 + 当日计划精简(2026-08-09)
- **用户需求(先对齐后执行)**:①当日计划和工地速览边框样式/文字大小统一;②当日计划不显示当天便签;③计划条目改成便签条目格式(时间保留)+ 标题日期下加周几
- **改动**:①`.cal-daypanel` border-radius 14px→`var(--radius)`、shadow→`var(--shadow-sm)`(与 `.site-prev` 一致),两栏标题字号统一 16px(`.site-prev .nm` 15→16px);②renderDayPanel 删 notesHtml 整块 + `.dp-notes/.dn-*` CSS 全清(日历网格"有便签圆点"的 dayNotes 保留);③标题日期下加周几:`.dp-date` 纵向容器,h3 下 `.dp-week`(如「星期日」);④计划条目标签 `plan-tag.site` 工地标签用 rust 色(与便签条目工地标签一致)
- 实测:周几「星期日」显示在 8月9日 下、无便签区块、09:00/14:30 时间保留、#工地A 橙色标签、两栏圆角同为 10px、零 JS 报错
- 备份:沿用上一备份(本轮改动小,直接基于 v31 后文件)
- **v32 补充修复(同日)**:当日计划条目内容漏了完整显示——`.dp-item .txt` 原来 `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`(单行省略号),改 `word-break:break-word; line-height:1.5; white-space:normal`(参考 v27 便签条目多行完整显示);实测长内容 6 行完整显示、字体清晰、零报错



### v33 · 当日计划排版改版·方案B 上下分区(2026-08-09)
- **用户反馈**:当日计划内容挤中间、两边空 → 出 3 个可视化样张(A 单行优化/B 上下分区/C 时间徽标)让用户挑 → **选 B**
- **改动**:`.dp-item` 从单行 flex 改上下分区——`.dp-row1`(勾选框+时间+删除,margin-left:auto 删除贴右)+ `.txt`(独占整行,padding-left:26px 与勾选框对齐,line-height 1.6)+ `.tags`(标签独立一行)+ `.dp-imgs`(图片区);模板同步改结构,plan-tag 从 txt 内联移出到 tags 行
- 实测:上下两行结构、长内容完整显示不空旷、#工地A #木工 独立标签行、已完成条目勾选+删除线、零 JS 报错
- 样张文件:`当日计划排版样张.html`(项目目录,保留供以后参考)

### v34 · 便签瀑布流排版(2026-08-09)
- **用户反馈**:便签卡片"太松散、显得空,格式排版不好" → 沟通后出 A 紧凑/B 内容优先/C 瀑布流三方案,用户选 **C**(全文字对齐确认:①「改于」保留 ②瀑布流=左右交替、每列内时间倒序 ③空便签缩成「# 日期」占位条点击展开 ④图片入口收进「＋内容」二级菜单,已加图片仍显示在卡里)
- **改动**:①布局:`.notes-grid` 改 `align-items:start` + 新增 `.notes-col` 双列容器,`renderNotes` 把卡片按 `i%2` 交替分到左右列(每列独立高度、错落瀑布流,不再同行等高);②紧凑:卡片去掉 `min-height:130px`、padding 13→11px、gap 8→6px、textarea `flex:1 0 auto`→`flex:none` + `min-height:64px`→`38px`(高度只跟内容走);③空便签:`isNoteEmpty(n)`(无文字且无图)渲染 `.note-empty` 占位条「# 日期」,点击 → `expandedEmptyNotes.add(id)` 重渲染完整卡 + focus(Set 会话内有效,刷新后未写的空卡仍缩为占位);新建便签也进 Set 直接展开;④图片:`n-img-add` 从卡片常驻行移除,收进「＋内容」二级菜单(`note-addmenu` 含「＋文本块/＋图片」,点「＋内容」toggle,点外收起,操作后自动收起);⑤修复连带 bug:`scrollHeight` 增高在便签页 `display:none` 时得 0 被写成 `height:0px` 内容裁剪(旧版靠 flex-grow 掩盖)→ 增高加 `sh>0` 保护 + `switchPage('notes')` 时 `fitNoteTextareas()` 重算;加块后 focus 改查重渲染后的新 DOM
- 实测:混合数据(长短/多块/空卡/带图/已完成)两列错落(216~286px 各不同)、空卡 56px 占位点击展开聚焦、菜单开关/点外收起/操作后收起、「＋文本块」加块记时间、「＋图片」触发图片选择、旧数据形状(仅 text/带图空文)→ 正常渲染、零 JS 报错
- 备份:`LUSEM的个人工作站-便签瀑布流前备份-20260809.html`
- **v34 补充微调(同日)**:用户反馈"添加内容与内容之间的空隙还可以压缩" → 卡片内 gap 6→4px(正文块/＋内容/标签/日期之间整体收紧,块内行距不动)、「＋内容」菜单内部按钮 gap 6→4px;备份 `LUSEM的个人工作站-便签间距压缩前备份-20260809.html`
- **v34 补充修复(同日)**:导航「工地」数字与工地数量对不上 → `$('#siteCnt')` 原来显示 `alerts`(工地提醒数,如阶段超期提醒)改为 `PROJECTS.length`(实际工地数,含已完工);`statAlert`(ALERT 统计卡)仍用 alerts 不变;验证 3 工地→导航 3;备份 `LUSEM的个人工作站-工地计数修复前备份-20260809.html`



### v35 · 首页美术配色四改(2026-08-09)
- **用户反馈(先对齐后执行)**:①日历当日填充色太深;②工地速览进度标签不明确没逻辑;③工地提醒不够显性+「今日/待办」要分色;④图标不好看 → 出 A/B/C 方案全文字对齐,用户拍板 **①A ②A ③B ④A**,并澄清「今日和待办」指顶栏统计(TODAY/ALERT/MEMO 配色),与 ③B 合并执行
- **①A 日历当日格**:`.cal-cell.today` 从整格实心 `--shell-deep`(深棕近黑)改 `--wood-soft` 浅木底 + `--wood-deep` 描边 + 深棕加粗数字;图例「今天」色块同步 wood-deep
- **②A 速览进度**:`.site-prev .st` 从 `NOW 阶段名` 改 `3/8 · 精交底` 阶段数字(`.idx` 暖橙加粗),完工 `8/8 · DONE`;色条从 8 色杂烩(`--s1..--s8`)改同系递进逻辑——已完成=`--wood` 橄榄棕、当前段=`--rust` 暖橙高亮+白描边、未完成=浅灰;`.stripe` 固定 8 列改 `grid-auto-columns:1fr` 按阶段数自适应(设计工地 6 段不再挤 6/8 宽);--s1..--s8 变量废弃保留
- **③B 提醒加日期分今日/待办**:跨阶段提醒 tag 新增 `due` 字段(YYYY-MM-DD,可空);`projAlertsSplit` 拆分——`due===todayStr()` → 今日(红),无 due 或非今天 → 待办(橙),**旧数据无 due 自动归待办**(兼容迁移);速览卡徽章 `今日 N`(rust 实底白字)+`待办 N`(浅橙),今日 0 时自动隐藏;工地首页卡 badge `今日 N · 待办 M`;看板 alertBanner 每个到达提醒 tag-link 带 `due-today`/`due-todo` class + 小徽章「今日/待办」;标签详情抽屉加「提醒日期」行(· 今日/· 待办/未设置·视为待办);编辑表单 isCross 时加 date 输入 + 说明文字,efSaveTag 保存 due
- **③C 顶栏三色**:statPlan→`#E5B65C` 琥珀金(今日计划)、statAlert→`#E08250` 暖橙(工地提醒,原有)、statNote→`#A8C070` 橄榄绿(待办便签)
- **④A 图标换 Lucide**:tab 三枚(日历=calendar、便签=sticky-note、工地=building-2)、品牌 mark=home、主题按钮=moon、备份=download、AI=sparkle 全换 Lucide 官方路径(24px 网格、stroke-width 2、round cap/join),删 tab 的 ✦ 星号叠字(`.ws-tab .star` CSS 清);备份菜单导出/导入/清空按钮已是 lucide 形状未动
- **误删修复**:patch 模糊匹配误吞无使用的 `crossVisible` 行 + 产生重复 `crossArrived` 声明 → 删重复行恢复(已 grep 确认 crossVisible 无任何调用)
- 实测:注入 3 工地(施工 3/8 有 due 今日/待办、设计 3/6 含无 due 旧数据、完工 8/8)→ 速览卡 `3/8 · 精交底`/`3/6 · 施工图`/`8/8 · DONE`,徽章 `今日 1 待办 1`/`待办 2`/`✓ 交付`;banner `#水电定位今日 #主材下单待办`;详情/编辑表单日期正确;改 due 保存后速览即时更新(今日 0 隐藏);顶栏三色 DOM 确认;深色主题正常;node --check 过 + 零 JS 报错;测试数据已清(保留 v2-ready)
- 备份:`LUSEM的个人工作站-v34改版前备份-20260809.html`(v35 全部改动基于此文件)

### v36 · AI 图标统一 + 便签 AI 改预判(2026-08-09)
- **用户反馈(先对齐后执行)**:①便签卡最左 AI 按钮还是 ✨ emoji,与其他 AI 图标不统一;②AI 不要"识别标签"了,要"根据便签内容预判需要注意的事项",联合工地主标签做冲突关联预警 → 方案对齐 **②A ③A ④A**(便签改预判、计划保留识别;只分析便签已关联的工地;卡片内建议列表),未关联工地时 AI 只给通用注意事项
- **图标统一**:便签卡 `n-ai`、计划浮层 `planModalAi` 两处 `✨` → Lucide sparkle SVG(与顶栏 AI 按钮一致);title 改「AI 预判注意事项」;快速条 `.dp-add.quick .ai-mini` CSS 为死代码未动
- **AI 改版(aiNoteTag → aiNoteAdvice)**:新增 `noteLinkedProjects(note)`(便签标签精确匹配工地名/客户/位置)→ 组装工地上下文(当前阶段/阶段标签/跨阶段提醒含 due 日期+重要标记)给 DeepSeek;system prompt 重写——输出 `{"advice":[{"type":"conflict|remind|notice","text":"..."}]}` 最多 3 条,宁缺毋滥:便签事项与工地提醒/阶段冲突→conflict(红)、跨阶段提醒相关且日期临近→remind(橙)、其他→notice(木色);未关联工地→user 消息注明"该便签未关联任何工地",AI 只给通用注意事项
- **渲染**:`.ai-advice` 条目 `flex-basis:100%` 独占整行(不干扰计划区 chips 横排),类型徽章三色(冲突=rust 实底白字/提醒=浅橙/注意=wood-soft);空结果显示「✦ 没有需要特别注意的事项」;AI 菜单说明文字同步更新(便签预判+计划识别标签)
- **计划区 AI 保留**:`aiSuggestTags`/`aiPlanTag` 不动(计划输入仍识别标签贴入)
- 实测:mock callDeepSeek 验证——关联工地便签(麓山别墅/精交底阶段/跨阶段提醒含日期)→ 3 条建议正确渲染(冲突/提醒/注意徽章),user 消息含完整工地上下文;未关联工地便签 → user 消息"该便签未关联任何工地";空 advice → 提示语;旧数据便签(无 tags 字段)→ 正常;node --check 过 + 零 JS 报错;测试数据已清(保留 v2-ready)
- 备份:`LUSEM的个人工作站-v35改版前备份-20260809.html`(v36 全部改动基于此文件)

- **v36 补充修复(同日)**:①**AI 按钮隐形 bug**——用户反馈"AI 按键跟背景融合到一起了":`.n-ai svg`/`.ai-mini svg` 缺 `stroke:currentColor`(Lucide 是 stroke 描边图标,默认 stroke:none → 笔画不渲染,图标完全不可见;`.n-del svg` 一直有该规则所以删除按钮正常);补两行 CSS 修复。②**融合显示(用户选方案 A)**:AI 一次分析返回两部分——注意事项列表 + 建议标签 chips(`{"advice":[...],"tags":[...]}`),prompt 规则⑥新增"给 1-3 个建议标签";渲染在预判列表下方加 `建议标签:` 行,chips 点击贴入(复用 ai-note-chip 事件),已存在的标签自动过滤;空结果仍显示「✦ 没有需要特别注意的事项」。实测:图标 stroke=ink-3 可见、3 条建议+chips 正确渲染、chips 点击写入 localStorage 标签、去重过滤生效、node --check 过 + 零报错

- **v36 补充修复(同日)**:**
便签文本只占左侧 140px、右侧大片空白**——`.note-card textarea` 是 replaced 元素,flex column 的 `align-items:stretch` 对它不生效,又没有显式 `width`,保持浏览器默认宽度(~140px);v34 改 `flex:1 0 auto`→`flex:none` 后宽度不再被拉伸,此问题被 v34 引入(用户 8-09 晚反馈)。修法:`.note-card textarea` 加 `width:100%`(全局已有 box-sizing:border-box)。实测:textarea 140px→478px(卡片 506px - padding),占满内容区,视觉确认右侧无空白

### v37 · 当日计划可编辑 + 便签正文暗红棕(2026-08-11)
- **用户反馈(先对齐后执行)**:①当日计划条目只能勾选/删除,内容不能改;②多个红色工地标签排在一起时,便签里面的内容(正文)不好读 → 出 A/B/C 方案全文字对齐,用户拍板 **1A 2B 3对**(计划=悬停 ✏️ 编辑只改时间+文字;便签正文=暗红棕 #3A2417;工地标签样式/排列不动)
- **① 计划条目内联编辑**:renderDayPanel 条目 `.dp-row1` 时间后加悬停 ✏️ 按钮(Lucide pencil 内联 SVG,复用 `.del` 悬停显隐模式);点击后 `replaceWith` 把 `.time`/`.txt` 换成 `.dp-e-time` input + `.dp-e-txt` textarea(placeholder「全天」),加 `.editing` class;**Enter 保存 / Esc 取消 / 失焦保存**;失焦时 `relatedTarget` 在条目内按钮(编辑/删除/勾选)则交给 click 处理不保存;编辑中再点编辑按钮=保存退出;文字清空保留原内容;保存/取消**先移除 editing class 再 renderCal**(否则焦点元素被 DOM 移除时触发 blur 误保存——实测踩坑,Esc 取消的值会被存进去);toggle/del 逻辑零改动
- **② 便签正文暗红棕**:新增 `--note-ink` 变量,浅色主题 `#3A2417`(暗红棕,带红调跟红色工地标签呼应);`.note-card textarea` 颜色改 `var(--note-ink, var(--ink))`;**dark/brutal/cyber 三主题补 `--note-ink:var(--ink)` 回退**(否则深色底上 #3A2417 几乎不可见——实测踩坑);done 状态仍 `--ink-3` 划线不受影响;工地标签 chip 样式未动
- 实测:注入新旧两轮数据——①编辑保存(改时间+文字→localStorage 生效+退出编辑态)、Enter 保存、**Esc 取消(改的值不保存)**,再点编辑=保存退出、编辑态点删除正常删除、勾选 toggle 正常、旧形状数据(便签无 texts/textDates、计划无 images)正常;②四主题正文字色 DOM 确认——light=rgb(58,36,23)=#3A2417、dark=#E6DCC6、brutal=#2E2B26、cyber=#D8E4FF 全回退正常,done 便签灰字+划线正常;vision 双轨确认便签页红标签+正文无溢出遮挡、日历页布局协调;node --check 过 + 零 JS 报错;测试数据已清(保留 v2-ready)
- 备份:`LUSEM的个人工作站-计划可编辑+便签字色改版前备份-20260811.html`

### v38 · 便签正文深锈棕 + 备份导出压缩图片(2026-08-11)
- **用户反馈(先对齐后执行)**:①v37 的便签正文字色"没变"(实际生效但 #3A2417 与原深棕 #2E261A 太接近,肉眼分不出)+ 说"字体"也要动 → 方案 A/B/C 全文字对齐,用户拍板 **1A 字号也调 2A**(正文深锈棕 #8A3A12 明显偏红 + 14px/字重500;备份导出自动压缩图片)
- **① 正文深锈棕**:`--note-ink` 浅色主题 `#3A2417` → `#8A3A12`(与便签标签选择器"工地"选项同款色,和红色工地标签同色系、暖木底上清晰);`.note-card textarea` 字号 13.5px→**14px**、加 `font-weight:500`;dark/brutal/cyber 回退 `var(--ink)` 不变
- **② 备份导出压缩**:新增 `shrinkBackupImg`(dataURL → canvas 重采样,最长边 1200px、JPEG 质量 0.75、**透明 PNG 垫白底**防变黑)+ `compressBackupPayload`(深拷贝 cal/notes,遍历所有 `data:image` 并发压缩,失败保留原图);导出事件改 async,payload 用压缩后的副本,**原 localStorage 数据不动**;toast 加「图片 N 张已压缩」提示
- 实测:注入 2000×1500 PNG(95,198B)便签 → 压缩后 1200×900 JPEG **14,691B,体积减 85%**,prefix `data:image/jpeg;base64,`;导出按钮端到端 toast「图片 1 张已压缩」;正文 DOM 确认 color=rgb(138,58,18)=#8A3A12、14px、500;vision 确认红棕清晰易读+红标签正常;旧数据无 images 正常;node --check 过 + 零 JS 报错;测试数据已清(保留 v2-ready)
- 备份:`LUSEM的个人工作站-v37改版前备份-20260811.html`(v38 全部改动基于此文件)

### v39 · 顶栏品牌位换 LUSEM 品牌 LOGO(2026-08-11)
- **需求(先对齐后执行)**:用户提供品牌 LOGO 素材(`Desktop\AI工作站\LUSEM品牌LOGO\`),要求替换个人工作站 LOGO;先调研给出位置分析——LOGO 为透明底**黑字**书法(「家宅有度 居想室成」+ BUILD YOUR HOME · FROM YOUR LIFE + DESIGN BY **LUSEM** 红字),顶栏是深棕褐深壳,黑字直接放会隐形 → 方案 A(米色圆角底块+横版/标准版 LOGO)/ B(金属质感直放)/ C(源脚本生成深壳适配版);用户拍板 **1A 2完全去掉只留 LOGO**;横版 vs 标准版合成对比后选**横版**(书法字大、红 LUSEM 醒目,标准版 44px 下英文糊成一团)
- **素材预处理**:横版 1961×460 → 裁透明边(1346×310)→ 缩 88px 高(2x 高清)→ 合成到米色 #F4EEE2(=浅色主题 --bg)底块(pad 16)→ JPEG q86 → **base64 内嵌**到 HTML(13.2KB,保持单文件特性)
- **代码改动**:`.ws-brand .mark`(34px 房子图标)+ `.name`(LUSEM 个人工作站/WORKSTATION 两行字)全部删除 → `<img class="ws-logo" src="data:image/jpeg;base64,...">`;`.ws-logo` 基础规则 height:44px / border-radius:10px / box-shadow 浮起感;**brutal**=直角 0 + 2.5px 粗黑边(替换原 .mark 白底黑边规则)、**cyber**=直角 0 + 1px 青色描边;顶栏 padding 13px→10px(44px LOGO 撑高,保持总高);**零 JS 改动**(node --check 过)
- 实测:四主题 DOM 确认——light/dark=radius 10px 无边框、brutal=border 2px #2E2B26 + radius 0、cyber=border 1px #00F0FF + radius 0(computed style 数字说话,vision 对细边框漏看是已知现象);vision 确认米色挂牌+黑字书法在深壳上清晰协调、顶栏高度适中;console 零错误;测试数据无需注入(纯视觉改动)
- 备份:`LUSEM的个人工作站-LOGO改版前备份-20260811.html`
- **当日追加(用户反馈后二轮改)**:①日历页 hero 下方 slogan「✦从图纸到落地的每一站」→ **「✦ BUILD YOUR HOME · FROM YOUR LIFE」**(英文一行,用户拍板一行不要两行,保留 ✦ 与深壳条样式);②挂牌图**重新生成只留书法大字**(横版图按列密度分析定位 x=900 书法/英文分界,裁掉右侧英文小字,挂牌 152px→**105px** 宽);③挂牌阴影按主题适配——浅/深=柔和投影加深(0 3px 10px .35 + 0 1px 2px)、**brutal=硬阴影 4px 4px 0 #2E2B26**(vision 确认"剪纸粘贴"立体感)、cyber=霓虹辉光(0 0 14px+.45 / 0 0 28px+.2 青色);实测四主题 DOM 确认 shadow/border/radius 全对,console 零错误
- 备份(二轮前):`LUSEM的个人工作站-LOGO改版前备份-20260811.html`(同一备份,v39 两轮改动均基于此)
- **当日三轮(用户反馈"挂牌放大150%,阴影看不清")**:①挂牌 **44px→66px(放大150%)**,挂牌图重新生成 2x 高清(132px 高,块 431×180,显示 158×66),顶栏 padding 10px→6px;②**阴影看不清根因**:黑色系阴影叠深棕褐深壳(浅/深主题 0 3px 10px .35 和 brutal 黑色硬阴影在深底上几乎不可见)→ 修复:浅/深=深投影加重(0 4px 14px .5)+**浅色描边 1px + 暖色光晕**(rgba(255,248,232,.3)+rgba(255,242,214,.25),深壳上清晰);brutal 先试米色硬阴影 #F4EEE2 **翻车**(和挂牌底同色=看不出阴影,vision 实测"扁平")→ 改**暖橙硬阴影 5px 5px 0 #B85C50**(米底+黑边+暖橙影三色粗野碰撞,vision 确认剪纸层叠感);cyber 辉光加强(0 0 16px .6 + 0 0 32px .3);实测四主题 vision+DOM 双轨全过,console 零错误
- 备份:`LUSEM的个人工作站-LOGO改版前备份-20260811.html`(v39 三轮均基于此)
- **当日四轮(slogan 字体)**:①用户提出挂牌背景做"横向抽帧"效果——出了 3 版残影样张(短/中/长拖影,前景清晰书法+右侧淡墨错位残影),用户否了("审美太差,保留原来的")→ **挂牌保持三轮定稿的米色底+四主题阴影,不抽帧**(样张已清理,HTML 未动过挂牌);②slogan 字体 `font-family:var(--serif)`(宋体系)→ **`"Times New Roman", var(--serif)`**(LOGO 英文同款,build_logo.py FONT=times.ttf);实测 document.fonts.check=true + canvas 测量宽度 221.3 vs 宋体 198.0(字体确实切换),vision 误读"无衬线"以 DOM 数字为准;console 零错误

- 教训:挂牌位图效果(残影/纹理类)先出小样给用户验收再动 HTML,用户审美偏好克制干净,过度的"设计感"(拖影/抽帧)会被否
- **当日五轮(slogan 配色字号)**:slogan 颜色 `var(--shell-weak)` 浅米弱色 → **#E5B65C 琥珀金**(=顶栏 stat「今日计划」金色,浅色主题 hero 区最显眼的黄,注释"今日计划 · 琥珀金"),字号 12px→**9.6px(-20%)**,字距 6px 保持;实测 DOM color=rgb(229,182,92) 与 .stat.plan .n 完全同色、fontSize=9.6px;vision 误读"白色/绿色"以 DOM 为准;console 零错误

### v40 · 完成沉底排序(2026-08-12)
- **需求(先对齐后执行)**:用户提出"便签里点了完成的项目,直接排在最后面,把未完成的优先放在前面"+ 让我检查所有内容适用逻辑 → 先排查现状(便签页 renderNotes 按创建顺序排、完成不换位;当日计划 renderDayPanel 同;工地页 projRank 已 finished=99 沉底,**无需改**);用户拍板 **便签 + 当日计划一起改**
- **代码改动(2 处,均一行排序)**:①`renderNotes()`:`notesData.filter(noteMatches).sort((a,b)=>(a.done?1:0)-(b.done?1:0))`——未完成在前、完成沉底,**稳定排序组内保持创建先后**;②`renderDayPanel()`:`visItems` 后接 `.slice().sort(...)`(**先 slice 再 sort,防直接改 calData 原数组顺序**)
- 实测:注入 5 便签+4 计划混合数据——当日计划 DOM 顺序=未完成2条→完成2条 ✓;便签瀑布流双列**每列内部**未完成在上、完成在下(左列未未完 / 右列未完)✓;点"完成"实时沉底 ✓、取消完成回到前面 ✓;当日计划勾 ✓ 实时沉底 ✓;console 零错误;node --check 过;测试数据已清(保留 v2-ready)
- 说明:**双列瀑布流下"沉底"是每列内部生效**(不是全局上下分区),两列各自未完成在上、完成在下,这是瀑布流布局的固有特性,用户已知晓
- 备份:`LUSEM的个人工作站-完成沉底改版前备份-20260812.html`

### v41 · 第四大页「客户」+ 速览分组排序(2026-08-14)
- **需求(先对齐后执行,grill-me 4 问)**:用户提出工作站大升级——①顶栏新增「客户」大类,分**未签客户**+**已签设计客户**;②已签设计客户跟工地里 type=design 绑定;③未签客户在工地页**新开一列**;④日历工地速览**设计排施工后**;⑤客户大类先做**两个美术模板**给用户选再执行
- **对齐决策(用户逐项拍板)**:①数据关系=**A 数据联动**(已签客户从设计工地自动聚合不重复录,未签客户一键转签约生成设计工地);②工地页形态=**A 总览第三段**(施工/已签设计/未签客户 三段卡片区);③未签客户字段=**B 完整版(CRM 式)**(客户名/电话/意向需求/来源/创建日期/跟进状态 待跟进·已报价·谈拢中/预算/备注);④细节=**A 全部按推荐**(转签约→生成已签设计工地带入客户名+备注;已签客户卡显示绑定工地名+当前进度;顶栏客户 tab 带计数;速览完工沉底)
- **美术模板 2 选 1**:做出 `美术样张/客户页美术模板-对比.html`(A 编辑档案感=延续米底+mono 英文标签+暖橙;B 中古档案册=暖木底+档案编号 NO.xxx+描边状态章+硬边卡片)——**用户选 A「编辑档案感」**,与现有三页视觉语言统一
- **代码改动**:
  - 顶栏第 4 tab「客户」(`data-page="clients"`,users 图标,`#clientsCnt` 计数=未签数+已签聚合数);`PAGES` 加 'clients';`switchPage` 切到客户页时 `renderClients()`
  - 新增 `workstation-clients` 数据域 + `CLIENTS`/`saveClients()`;字段 `{id,name,phone,need,source,date,status,budget,note}`
  - 客户页 `#page-clients`(模板 A 风格 `.cl-wrap/.cl-card/.cl-st/.cl-signed`):未签客户 CRM 卡(编辑/删除/转签约)+ 已签设计客户自动聚合卡(`signedClientGroups()` 按 client 名分组,同名多工地合并,每工地一行=名称+进度条+`x/6 · 阶段`)
  - 客户编辑浮层 `#clientModal`(复用 plan-modal 样式,独立 modal 不干扰工地 drawer;客户名必填校验)
  - 转签约 `signClient()`:生成 `{type:'design', name:客户名, client:客户名, globalTags:[{note:需求·预算·备注}]}` → 客户从未签列表移除 → 跳新工地看板
  - 工地页 `renderSiteHome()` 加**第三段「未签客户」**(复用 cl-card,点击→客户页,卡上「转签约」直接签约);`#projGrid` 点击委托加 site-sign/site-client 分支
  - 日历速览 `renderSitePreview()` 排序:`siteSortKey`(施工=0/设计=1) + `siteRank`(未完工=阶段索引大在前,完工=-1 **沉底**)
  - 备份导出/导入加 `clients` 字段(导入旧版备份缺省保留现有);`clearAllData`/V2 初始化清 `LS_CLIENTS`
- **实测(浏览器注入测试数据)**:速览顺序=施工→设计未完工→设计完工沉底 ✓;客户页 3 未签+2 已签渲染 ✓;新建客户(周先生 已报价)保存 ✓;编辑预填 ✓;删除确认 ✓;转签约=CLIENTS 3→2 + 自动生成 design 工地「陈女士」并跳看板 ✓;已签客户点工地行直达看板 ✓;搜索按名/电话/需求过滤 ✓;四主题 light/dark/brutal/cyber 客户卡样式全部适配 ✓;旧数据回归(无 clients key + 旧工地形状)启动零报错 ✓;空状态文案 ✓;console 零错误;node --check 过
- 说明:**已签设计客户不落独立数据**,永远与工地 type=design 实时一致;工地编辑里把 type 改成「设计」或「施工」,客户页聚合自动跟随
- **当日二次调整(用户反馈)**:①顶栏顺序「客户」移到「工地」左边(日历/便签/**客户**/工地)——用户原话「把客户总标签,放在工地的左手边」;②**已签设计客户卡去掉看板式进度**(进度条/阶段/工地行跳转全删),卡片只留「客户名 + 设计徽标 + 注意事项」——注意事项来自该客户设计工地的 globalTags 备注(转签约时自动带入的需求/预算/备注);用户原话「已签客户不需要在客户里面展示看板,这个页面主要是记录注意事项」;`clientStripeHTML` 改为只提取备注文本;实测:已签卡 DOM=名称+徽标+备注文字,无 srow/sbar,console 零错误;备份:`LUSEM的个人工作站-客户页精简调整前备份-20260814.html`
- **当日三次调整(用户反馈:「客户里面内容跟便签和日历联动 + 内容可增加和二次编辑」)**:①**客户↔便签/日历联动**——新增 `clientRelatedRecords(name)`:便签/日历里 `#客户名` 标签精确匹配 **或** 正文包含客户名(≥2字防误判,如「陈女士」「张先生」)的条目自动汇聚到客户卡「关联记录」区,点击跳转:计划→日历定位日期、便签→便签页定位卡片;②**内容块系统**——每张客户卡「＋ 内容」手动追加多条内容块(存 `c.blocks=[{id,text,date}]`,带 `M月D日 HH:MM` 时间戳),块 hover 出现 ✏️编辑/🗑删除,复用便签多文本块交互;③**转签约改标记不删除**——`signClient` 不再 `CLIENTS.filter` 移除,改为 `c.signed=true; c.projectId=proj.id`(未签区过滤 `!c.signed`),已签客户从工地聚合 + CLIENTS 记录合并,内容块/备注跨签约保留;已签卡无记录时点「＋内容」自动创建 signed 记录(`clientRecForBlocks`);④导入清洗保留 `signed/projectId/blocks` 字段;顶栏计数 `!signed 未签数 + 已签聚合数`;工地页第三段未签客户过滤 `!c.signed`;⑤实测:未签卡陈女士 2 条关联(计划正文+便签标签)、张先生 2 条(便签正文+计划标签)、已签卡李先生内容块保存(自动建 signed 记录)、关联记录点击跳日历定位 2026-08-14 / 跳便签定位 n1、转签约后陈女士从未签区移到已签区且内容保留、console 零错误;**踩坑:已签卡无内容块时没有 `.cl-blocks` 容器,「＋内容」插入锚点 null 报 TypeError——修法:已签卡始终渲染空 `.cl-blocks` 容器**;备份:`LUSEM的个人工作站-客户联动内容块改版前备份-20260814.html`
- **当日四次调整(用户反馈:「便签关联的工地没在客户-已签设计显示 + 已签客户姓名后显示工地名」)**:①**关联记录按「客户名+绑定工地名/位置」匹配**——`clientRelatedRecords(name, extraKeys)` 增加附加 key:标签精确匹配客户名或工地名/位置,正文包含任一(≥2字);已签卡调用时传入该客户所有 design 工地的 name+loc(`extraKeys`),便签/日历里打 `#麓谷和苑8-502`(工地标签)也汇聚到李先生卡;②**已签客户卡姓名下加「绑定工地 · xxx」行**(`.tie` 样式,多个工地 `、` 分隔,只显名称不显进度);实测:李先生卡=「李先生 设计」+「绑定工地 · 麓谷和苑8-502」+ 关联记录 2 条(工地标签便签+客户名便签),施工工地便签(#云栖玫瑰园)不误入已签区,vision 视觉确认,console 零错误;备份:`LUSEM的个人工作站-客户工地名关联改版前备份-20260814.html`
- **当日五次调整(用户反馈:「日历和便签里已完成的,客户关联记录也自动变灰/划线」)**:`clientRelatedRecords` 返回带 `done: !!p.done / !!nt.done`;`clientRecHTML` 渲染 `done` → `.cl-rec-item.done`(opacity .5 + 全文 line-through + 来源徽章加 ✓,hover 恢复 .75);未签卡/已签卡、计划/便签、工地标签记录全部生效;实测 DOM:已完成记录 done 类+opacity 0.5+line-through+「计划✓/便签✓」,未完成正常色,vision 确认灰划线与正常色对比明显,console 零错误;备份:`LUSEM的个人工作站-关联记录完成态改版前备份-20260814.html`
- **当日六次调整(用户反馈:「客户关联信息排序同样执行已完成排后 + 全站统一排序逻辑,自查」)**:自查发现 3 处不一致并全部统一——①**客户关联记录** `clientRelatedRecords` 排序加 `(a.done?1:0)-(b.done?1:0)` 前置(未完成在前、完成沉底,组内日期倒序);②**工地看板关联记录** `siteRelatedRecords` 同样加 done 沉底(且补上 `done` 字段,渲染 `.tl-item.done` 变灰划线+✓,与客户页统一);③**发现并修复工地总览排序 bug**:`projRank` 原 `finished ? 99 : stageIndex` 降序排列导致**完工排最前**(与「沉底」相反),改为 `finished ? -1` → 进行中在前、完工沉底;实测:工地总览=施工进行中A→施工完工B→设计进行中C→设计完工D ✓;客户关联记录=未完成(08-14→08-12)在前、完成(08-14→08-13)沉底 ✓;便签/当日计划/日历速览原本已沉底无需动;console 零错误;备份:`LUSEM的个人工作站-完成沉底全站统一改版前备份-20260814.html`
- **当日七次调整(用户反馈:新建客户内容优化——楼盘/风格=标签选择、注意事项/跟进进度=便签式时间水印、来源/备注取消)**:
  - **对齐(grill-me 3 问)**:①标签录入=**A 预置+可自定义**(楼盘/风格各预置常用选项可点选,手动输入新标签自动记住;客户专属标签**只在客户页显示**,不进全站 TAGS 栏);②跟进进度=**A 内容块时间线取代状态下拉**(每次跟进一条自动时间水印,最新在上倒序);③表单=**A 精简**(客户名/电话/楼盘标签/风格标签/预算,意向需求大文本框取消)
  - **代码改动**:①数据模型:客户记录加 `lots/styles`(标签数组)+ `notes`(注意事项块)+ `follows`(跟进进度块,带时间水印),新增 `LS_CLIENT_TAGS` 标签候选池(楼盘 5 预置+风格 8 预置);②**旧数据迁移** `migrateClient`:need→notes[0]、note→notes、status非pending→follows[0](「跟进状态: 谈拢中」),source 不再展示;③表单重写:删除 cfNeed/cfSource/cfStatus/cfNote,新增 cfLotInput/cfStyleInput + 标签点选面板(renderClientTagPicks,点选切换 on 态/回车添加自定义);④客户卡布局(从上到下):信息区(名/电话/标签/预算)→注意事项(＋内容)→跟进进度(＋跟进,倒序)→关联记录;⑤转签约带入楼盘/风格/预算/注意事项;⑥导入清洗保留新字段;⑦工地页第三段未签卡同步新布局
  - **实测**:旧数据注入→need 变注意事项、status 变跟进记录 ✓;新建周先生:点选 #云栖玫瑰园 + #奶油风 + 输入自定义 #法式 保存 ✓,卡片标签显示可删 ✓;跟进进度 2 条倒序(最新在上)+ 时间水印 ✓ 编辑 ✓;vision 确认标签/预算/注意事项/跟进时间线布局;console 零错误;**踩坑:新建客户时 clientEditRec 为 null 导致标签点选无效——修法 openClientModal 新建态给 `{lots:[],styles:[]}` 临时对象**
  - 备份:`LUSEM的个人工作站-客户表单改版前备份-20260814.html`;**注意:工作站目录已从 `01_项目` 重命名为 `01-项目`(下划线改连字符),技能/日志中的旧路径需同步**
- **当日八次调整(用户反馈:新建客户再优化——电话取消、标签可增删、编辑表单内删除客户)**:
  - **对齐(grill-me 3 问)**:①电话=**A 取消**(表单不填、卡片不显示,已存数据保留不删);②标签=**A 表单内可删+卡片可删**(表单已选标签带 × 点选取消,卡片悬停 × 删除);③删除=**A 表单内加「删除客户」按钮**(编辑时显示、新建时隐藏,确认后删除并关闭)
  - **代码改动**:①表单删 cfPhone 行,客户卡/工地页第三段删 `.ph` 电话行(数据字段保留);②`renderClientTagPicks` 已选(on)标签带 `<i class="x">×</i>` 可取消;③表单底部加 `#cfDel`「🗑 删除客户」按钮(openClientModal 编辑态显示/新建态隐藏,点击 showConfirm 确认后从 CLIENTS 删除+关 modal+刷新);④**踩坑:上一轮表单改版时 clientCardHTML 重构误删了 `.cl-act` 编辑/删除/转签约按钮组(实测卡片无编辑入口)——本轮补回**
  - **实测**:电话隐藏(card 无 .ph、表单无 cfPhone)✓;编辑表单 delBtn 可见、新建隐藏 ✓;已选标签带 × 点选取消(on 0)✓;表单删除客户流程(确认→CLIENTS 0→modal 关闭→列表刷新)✓;卡片编辑/删除/转签约按钮恢复 ✓;console 零错误
  - 备份:`LUSEM的个人工作站-客户表单电话标签优化改版前备份-20260814.html`
- **当日九次调整(用户反馈:「原始标签无法删除 / 编辑内容时容易回主界面 / 弱化编辑和删除标签-转签约放最下面」)**:
  - **对齐(grill-me 1 问)**:弱化形态=**A hover 才显示**(编辑/删除/标签删除平时透明隐藏,悬停卡片淡入;转签约沉底全宽固定显示)
  - **代码改动**:①**预置标签可删**——`renderClientTagPicks` 未选预置标签也带 ×(`data-role="del-preset"`),点 × **直接删除不弹窗**(用户反馈「标签删除不用弹窗,直接删除」,去掉了 showConfirm,直接从 `CLIENT_TAG_POOL.lots/styles` 移除并持久化;已选客户不受影响);已选标签 × 仍是取消选择(`data-role="unpick"`),两角色区分;②**遮罩误触不关闭**——注释掉 `clientModal` 的 `e.target===mask→close`,编辑一半误点遮罩不再丢内容(只能点取消/× 关闭);③**弱化编辑/删除**——`.cl-act` 加 `opacity:0;transition`,`.cl-card:hover .cl-act{opacity:1}` 淡入;④**转签约沉底**——新增 `.cl-sign-row`(全宽按钮行,`.cl-sign-btn` width:100%),未签客户卡与工地页第三段卡的转签约按钮从 `.cl-act` 移到卡片最底部
  - **实测**:cl-act 非hover opacity=0、CSS 规则 `.cl-card:hover .cl-act{opacity:1}` 存在 ✓;转签约 `.cl-sign-row` 沉底 display:block ✓;点击遮罩 modal 仍打开 ✓;点预置标签「麓谷和苑」×→确认→池中移除(lots 5→4)✓;console 零错误
  - 备份:`LUSEM的个人工作站-按钮弱化转签约沉底改版前备份-20260814.html`
- **当日十次调整(用户反馈:「新客户姓名后面加业务员 + 楼盘地址标签选中颜色不变/点击直接删除,自己审查下」)**:
  - **对齐(grill-me 1 问)**:标签删除范围=**A 表单选择区点标签=删候选池**(输入框回车=添加;卡片悬停 × 保持现状)
  - **代码改动**:①**业务员字段**——表单加 `#cfSales` 输入框(客户名后),数据模型加 `sales`,客户卡姓名后显示 `.sales` 小字(`陈女士 · 小王`,橄榄色等宽小字,不抢主名),已签卡同样显示,导入清洗保留 sales;②**表单标签交互重写**——去掉 on 高亮变色切换和 × 图标,`renderClientTagPicks` 标签统一样式(已绑定客户仍加 on 类区分),**点击标签本体=直接删除**(从 CLIENT_TAG_POOL 移除 + 当前编辑客户 lots/styles 解绑,不弹窗,toast 提示),输入框回车=添加(进候选池+绑定客户)
  - **实测**:卡片姓名行「陈女士 · 小王」✓;表单业务员预填/新建保存(sales=小李)✓;点击标签[云栖玫瑰园]→5→4 无确认弹窗、池中移除 ✓;保存后 lots 已解绑 ✓;vision 确认姓名+业务员小字协调 ✓;console 零错误
  - 备份:`LUSEM的个人工作站-业务员字段标签点击删除改版前备份-20260814.html`
- 备份:`LUSEM的个人工作站-客户大类改版前备份-20260814.html`
- PLAN:`PLAN.md`(grill-me 对齐产物,五段式)

### v42 · 自动备份(2026-08-23,用户拍板 B+3)
- **功能**:数据变更后自动下载 JSON 到电脑,当天只留最新一份——`save()` 通用函数挂钩子(任何数据保存后防抖 8 秒触发),打开页面有初始化 save 也会自动备当天第一份(行为=打开即有一份当日档);文件名 `个人工作站自动备份-YYYY-MM-DD.json`(与手动导出的 `个人工作站备份-YYYYMMDD.json` 区分);当天去重=`workstation-auto-backup` 存 `{date,last}`,date=今天直接 skip 不重复下载;备份菜单顶部新增 `.menu-status` 状态行(`#autoBkStatus`:「自动备份已开启 · 今日 HH:MM 已备份 ✓/今日尚未备份」),打开菜单时 `updateAutoBackupStatus()` 刷新
- **对齐**:clarify 2 问(备份存哪/什么时候存)→ 用户选 **B 自动下载到电脑 + 3 数据变更后自动存(当天只保留最新一份)**
- **代码改动**:①`save(k,v)` 加 `try{scheduleAutoBackup();}catch(_){}` 钩子(异常不影响原保存);②新增 `scheduleAutoBackup`/`runAutoBackup`/`updateAutoBackupStatus` + `LS_AUTOBK`/`AUTOBK_DEBOUNCE(8000)`;③记录 LS_AUTOBK 用 `localStorage.setItem` 直写(不走 save,防递归钩子);④`window.__autoBkLog` 便于测试;⑤复用 `compressBackupPayload()`(图片压缩),payload 结构同手动导出(calendar/notes/projects/clients/site/hiddenTags + auto:true)
- **实测**:预览窗格打开页面 → toast「已自动备份 ✓ 2026-08-23 18:55」→ 重开页面 10 秒后无新 toast(当天去重生效,时间仍是 18:55 非新时间)✓;node --check 语法通过 ✓
- 备份:`LUSEM的个人工作站-自动备份改版前备份-20260823.html`

### v42.1 · 全量代码审查 + 优化(2026-09-13,用户:「深度审查所有代码,先优化」)
- **审查范围**:全文 4319 行(样式 1198 行 / HTML 320 行 / JS 2800 行),含 744 条 CSS 规则、152 个 JS 函数/常量声明
- **审查结论**:代码底子良好(数据层统一 `load/save`、无 TDZ 引用错误、无未声明变量、CSS 无冲突重复、`node --check` 通过);主要问题是历史改版留下的**死代码**和导入链路的**字段丢失缺陷**
- **发现并修复的 2 个数据丢失缺陷(导入 JSON 时)**:
  1. **工地类型丢失**——导入清洗 `cleanProj` 未保留 `type` 字段,紧随其后的 `migrateStageIds`(`p.type !== 'design'` 即赋 `'build'`)把**所有已签设计工地强制降级为施工工地**:阶段体系从「量房/确定平面/施工图/效果图/预算/签施工」错成「开工/拆砌墙/精交底/泥工/木工/油漆/安装/软装」,`curStage:'layout'` 一并被重置为 `start`,客户页「已签设计客户」聚合(按 `type==='design'`)同时塌掉。修法:清洗时显式写回 `type`
  2. **便签多内容块丢失**——导入清洗 `cleanNotes` 未保留 `texts`/`textDates`(v30 多块结构),导入后每张多块便签塌成单块,块内时间水印**不可逆丢失**(导出侧 `compressBackupPayload` 是深拷贝,字段本来在备份文件里,是导入侧丢的)。修法:有 `texts` 就原样保留,并按既定约定重算 `text = texts.join('\n')`
  - **验证**:Node 沙箱 + jsdom 真跑,**修复前**复现(design→build、texts→undefined),**修复后** 9 项断言全过;旧数据(无 type 工地、无 texts 单块便签)回归不受影响
- **死代码清理(共 -4863 字符)**:
  - **CSS 删除 55 条规则 / 64 行**——`sc-*`(旧工地列组件)、`dp-add*`(旧当日计划快速输入行)、`cl-st`/`cl-signed`/`srow`/`sbar`/`snm`/`sst`(v41 已简化的客户状态章与进度条)、`stage-head`/`stage-name`/`stage-state`/`st-done`/`st-now`/`tag-list`/`empty-hint`/`stage-col.future`(旧阶段卡)、`cal-tag-picker`、`.cl-card .ph`、`.gt-pill .src .s-note/.s-plan/.s-site`、`.theme-btn .icon-sun/.icon-moon`、`.cc-ok.plain` 等
  - **JS 删除 5 个死函数 + 5 个死常量**——`findSiteByAlias`、`renderCalTagPicker`(依赖的 `#calTagPicker` 元素早已不存在)、`aiStageChecklist`(v36 已下线占位)、`stageIndex`/`stageName`/`tagByStage`(实际用的是 `stageIndexOf`/`stageNameOf`)、`projTypeName`、`CLIENT_STATUS`、`timeSelHTML`、未用变量 `pct`
  - **调试残留** `window.__autoBkLog`(v42 测试用)整组移除
- **其他优化**:①`esc()` 补单引号转义(`&#39;`,属性注入防护);②启动时 `renderSitePreview()` 重复调用(updateHeader 内已调)去掉一次
- **安全纪律**:删 CSS 前用**机器判据 + 人工复核**双重确认——首轮自动判据曾把 `.tag-card.cross.arrived`(arrived 由 `${isArrived?'arrived':''}` 动态生成)判成死规则,安全校验(「使用中的 class 不得失去唯一样式」)拦下后改用最严判据(该 class 在 CSS 外**任何位置都不出现**),并排除动态生成类(`s1..s8` 来自 `'s'+(i+1)`、`c-0..c-2` 来自 `c-${i%3}`);删除后逐条对账「55 条删除规则 / 0 条新增规则」,并确认 `t-input`/`gt-pill`/`theme-btn`/`idx` 等活 class 仍保有样式
- **回归实测(jsdom 真加载整页)**:32 项断言全过——启动零错误、旧数据迁移(need→notes / status→follows / electric→briefing)、四页渲染(日历 35 格 · 计划 2 条 · 便签 2 张 · 工地 3 张 · 客户 3 张)、顶栏计数、看板打开、save 钩子、自动备份状态栏、标签贴入、四主题切换、导出 payload 含 `type`+`texts`;**优化前后跑同一套用例结果完全一致(零行为回归)**
- 备份:`LUSEM的个人工作站-代码优化改版前备份-20260913.html`
- 遗留(本轮未动,留给下一轮):`syncTagChipState('calTagPicker', …)` 与 `inp.id==='newText'` 分支已无对应元素(有 null 守卫,无害);`STAGE_AI_CACHE` 是只写不读的死状态;全站 4 处 `(a,b)=>(a.done?1:0)-(b.done?1:0)` 排序比较器可抽成公共助手

### v42.2 · 死分支清理(2026-09-13,用户拍板「只清无元素残留分支 + STAGE_AI_CACHE,排序逻辑不动」)
- **背景**:v42.1 审查发现「当日计划快速输入条」整条交互链依赖的元素(旧版「极简快速条」入口)已在 v28+ 改版中移除,但 JS 分支还留着
- **核查**:`renderDayPanel()` 实际只渲染 `.dp-item` + `data-act` / `data-role="plan-tag|plan-img|plan-img-add"`;`#newText`/`#newTime`/`#calTagPicker`/`#aiPlanSuggest`/`#quickImgBtn`/`#aiPlanBtn` **在 HTML 里一个都不存在** → 相关分支全部不可达
- **清理内容(净减 2655 字符 / 119 行)**:
  - `#calDayPanel` 的 `input`/`change` 监听(只处理不存在的 newTime/newText)
  - `#calDayPanel` click 委托里的 6 个死分支:`quickImgBtn` / `tp-del-site` / `tp-del-ev` / `tp-add-*` / `.tp-chip` / `#aiPlanBtn` / `.ai-plan-chip`(这些元素只在**计划浮层** `#planModalTags` 里渲染,浮层有自己独立的委托,不受影响)
  - `keydown` 里的 `newText 回车=addPlan` 分支 + `addPlan()` 死函数
  - `stashPlanDraft()` / `clearPlanDraft()` 死函数(仅被上述死分支调用)
  - `toggleTagInInput` 的 newText 分支;`TIME_OPTS`(随 timeSelHTML 一起废弃)
  - **`STAGE_AI_CACHE`**(只写不读的状态)声明 + 两处清空赋值;`LS_AI_STAGE` key 保留在清空列表里(清理用户历史上残留的 `workstation-ai-stage`)
  - 同步注释:`/* 计划 AI 识别(快速条 newText / 浮层 planModalText 通用) */` → 去掉已废弃的快速条措辞
- **保留核对**:`stashModalDraft`/`syncTagChipState`/`toggleTagInInput`/`tpInlineAdd`/`deleteSiteTag`/`deleteEventTag`/`parsePlanInput`/`aiPlanTag`/`buildTagPickerHTML`/`triggerImgPick` 逐一确认仍有引用(≥2 处),未产生新的孤儿函数
- **实测(jsdom 整页,36 项断言)**:全绿——启动零错误、四页渲染、**当日计划委托交互(勾选切换完成态 / 点计划标签触发筛选 / 点删除移除条目 / 加图片按钮仍在)**、**计划浮层标签选择器(19 个 chip、点击贴入 `#标签`)**、工地看板、save 钩子、自动备份状态栏、四主题切换、导出 payload;**与 v42.1 版本跑同一套用例结果逐行一致(零回归)**
- **坑(重要,已记入技能)**:按行号删块之前,**必须先做完所有会改变行数的内容替换**,否则后续行号整体位移 → 删错行 → 大括号失衡、`node --check` 报 `Unexpected end of input`。正确顺序=「先按行删块(行号当场锁定 + 逐块首尾断言)→ 后做内容替换」;或全部改用内容锚点定位

### v42.3 · 修复 TAGS 圆点 + 死代码删除全量复审(2026-09-13,用户报「TAGS 标签有同步时,该标签里面的红黄蓝点不见了」)
- **根因(v42.1 的误删)**:TAGS 栏每个标签胶囊前的来源圆点,渲染处是
  `const dots = [...v.sources].map(s => \`<i class="s-${s}"></i>\`)`(`s` ∈ note/plan/site)
  —— 三个类名 `s-note`/`s-plan`/`s-site` 是**模板字面量动态拼出来**的,在 CSS 之外搜不到任何字面量。v42.1 的死代码判据只做字面量扫描,把这三条颜色规则(`--olive` 绿 / `--amber` 琥珀 / `--rust` 橙红)误判成死代码删掉了;而 `.gt-pill .src`(flex)和 `.gt-pill .src i`(5px 圆)还在,**圆点仍在渲染、只是全部透明无色**
- **修复**:按原版逐字恢复三条规则。已核对 `.gt-pill .src` 相关规则恢复后共 5 条(容器/圆点尺寸/绿/琥珀/橙红),与原版一致
- **全量复审(关键)**:用**「字面量扫描 + 从所有 `class="..."` 模板解析动态类名 + 规则整条不可达判定」三层判据**重新审计我删掉的全部 52 条规则:
  - 建立动态类名白名单(从 19 个含 `${}` 的 class 模板解析,含 `s-${s}` / `'s'+(i+1)` / `c-${i%NOTE_COLORS}` / 各三元分支 / `cls.join(' ')`)
  - 逐条判定:**选择器里只要有任一个 class 无法生成**(既非字面量也非动态),整条规则不可达、删除安全
  - 结论:**52 条全部安全,零其他误删**。唯一误删就是那三条圆点规则
- **实测(jsdom,43 项断言全绿)**:新增圆点专项——3 条颜色规则存在且值为 `var(--olive)`/`var(--amber)`/`var(--rust)`;TAGS 栏 4 个胶囊、5 个来源圆点;`#张总·云栖玫瑰园` 标签同时带便签+计划两个圆点;**「所有已渲染圆点都能匹配到一条颜色规则」= 0 个漏配**;其余 36 项(四页渲染/迁移/当日计划交互/浮层标签/主题/导出)保持全绿
- **交付**:同步覆盖用户桌面 `C:\Users\YANJiCG\Desktop\LUSEM的个人工作站.html`,md5 与 F 盘正本一致(`fc5b77070925…`)
- 备份:`LUSEM的个人工作站-代码优化改版前备份-20260913.html`(未变,仍是优化前原版)
- **教训(已写入技能)**:清 CSS 死代码时,字面量搜索远远不够——**必须先枚举所有 `class="..."` 模板里 `${}` 能拼出的类名**,否则会把"点还在但没颜色"这类**半残**状态删出来,比整块消失更难发现

### v43 · 新增第 5 主题「Apple · 玻璃白」(2026-09-13,调用 web-design-engineer 技能)
- **任务来源**:用户「我想调用最新加入的技能,重新更新一版风格样式,主要是采用苹果风格」
- **技能**:`web-design-engineer`(11:58 刚更新的版本,带 `references/brand-designs/` 74 品牌库)→ 读 `brand-designs/apple/DESIGN.md` + `redesign-protocol.md`
- **Step 0 事实核实(技能强制)**:WebSearch 确认 Apple **当前**设计语言 = **Liquid Glass**(WWDC25 发布,2025-09-09 更新指导),不是老的扁平卡片。关键条文:「UI 层(顶栏/tab/工具栏)用标准 Liquid Glass 材质、**去掉自定义背景色**;品牌色放**内容层**,只用于动作/状态/反馈」「**不要**给卡片/按钮/文字加阴影;层级靠面切换 + 玻璃」「全系统只有一个强调色 Action Blue #0066cc」
- **对齐(grill-me 式三点)**:用户拍板 **D1-B / D2-A / D3-A**
  - D1-B **完全苹果中性**:白 + #f5f5f7 + 近黑 + Action Blue,不留暖调
  - D2-A **新增第 5 主题**:原 4 主题(浅/深/粗野/赛博)一字不动,一键切换对比,零风险回退
  - D3-A **装字体**:拉丁/数字 = Inter(Apple DESIGN.md 官方点名的 SF Pro 开源近似),中文 = Noto Sans SC(苹方近似)
- **字体落地(免管理员 · 按用户级安装 9 个文件)**:
  - 源:`F:\HERMES\09-素材\字体\`(Inter v4.1 官方 Release + Noto CJK SubsetOTF/SC 静态字重)
  - 装:`$LOCALAPPDATA\Microsoft\Windows\Fonts` + `HKCU\...\Fonts` 注册表 9 条(Inter / Inter SemiBold / Inter Bold / Inter Display {Regular,SemiBold,Bold} / Noto Sans SC {Regular,Medium,Bold})
  - **坑**:Inter-SemiBold 在 Windows 上注册成**独立家族**「Inter SemiBold」(不是 Inter 的一个字重);Noto Sans SC 可变字体的默认实例是 **Thin**、家族名不规范 → 所以用静态字重 + `@font-face { src: local("Inter-SemiBold") }` **按 PostScript 名精确映射字重**(文件内 12 条 @font-face,只影响 Apple 主题)
  - 中文 600 → 映射 Noto **Medium**(CJK 里 Medium 相当于拉丁的 SemiBold,避免中文发粗)
- **主题实现(纯增量,零改 JS 逻辑)**:
  - `html[data-theme="apple"]` 变量块(token 到 token 映射:--shell 玻璃白 / --bg #f5f5f7 / --surface #fff / --ink #1d1d1f / --rust 与 --wood → Action Blue #0066cc / --olive → 系统绿 #34c759 / --amber → 系统橙 #ff9500 / --radius 18px / --shadow none / --serif → AppleDisplay 无衬线)
  - 结构覆盖:~150 行(顶栏毛玻璃 `backdrop-filter:saturate(180%) blur(20px)` / tab 去胶囊改 macOS 工具栏选中态 / Hero 白底 + 62px Display 负字距日期 / 卡片 18px 圆角 + 发丝描边 + **无阴影** / 按钮 pill + 按下 `scale(.95)` / 菜单与弹层玻璃 / 今天 = 蓝圆 26px 白字 / 阶段条圆头胶囊 / 页面淡入)
  - 接线:主题菜单加 `<button data-theme-opt="apple">Apple · 玻璃白</button>`、`THEMES` 加 `'apple'`、`THEME_NAMES` 加 `apple:'Apple'`(其余零改动)
- **验证(不用肉眼当权威,三重证据)**:
  1. **js 语法** `node --check` PASS
  2. **Playwright 真机截图 + 计算样式探针**:`document.fonts` 全部命中;tab 字体 = AppleSans w600、Hero = AppleDisplay w600 62px;卡片 = `#fff / 18px / box-shadow:none / 边框 #e0e0e0`;顶栏 = `rgba(250,250,252,.68) + blur(20px)` ✅
  3. **全站暖色自动扫描**(遍历所有元素计算样式,按 `r>g>b 且 r-b>24` 判暖色):逐轮揪出并清掉 4 类残留——便签卡日期/改于的金色 #C9A227、内容块时间水印金色、「新便签」「完成」按钮的米白字 #F2EAD6、「无提醒」的橙棕字 #c93400;并把 TAGS 来源圆点与日历圆点统一成同一套语义(**计划=蓝 / 便签=绿 / 工地=橙**)。**最终只余 2 处刻意保留的系统状态色**(工地提醒红 #ff3b30、工地来源橙 #ff9500)
  4. **回归**:五主题 × 四页 = 20 项全通、页面报错 0;`ws_warm.py light` 仍报 240 处暖色 → **原四主题完好未被污染**
  5. **AI 视觉评审**(vision 两轮):8.5/10,「气质高度统一,无杂色,无排版事故」
- **交付**:同步覆盖桌面副本(md5 `01ef3bf2b60d…` 双向一致);四页预览图存 `预览截图\Apple主题-四页总览.png`
- 备份:`LUSEM的个人工作站-Apple主题改版前备份-20260913.html`(267960 字节)
- **新增工具**(放 `F:\HERMES\08-工具与脚本\`):`venv-web`(Playwright 专用 venv)、`ws_shot.py`(截图+字体+五主题四页回归+错误捕获)、`ws_dom.py`(计算样式探针,排除肉眼误判)、`ws_warm.py`(全站暖色扫描)
- **重要教训**:①**视觉评审必须 DOM 计算样式为权威**——vision 在缩略图上把蓝底标签误读成橙底、把 `box-shadow:none` 说成"有弥散投影",同一张图放大后又读对;②反过来在第一轮总览图上 vision 报的"金色日期/米白字"是**真的**(DOM 探针首轮漏了 `.n-date`,是暖色扫描补上的)→ **两者互补,不能只用一种**

### v43.1 · 动态视频背景(仅 Apple 主题,2026-09-13,用户投喂 Mux HLS 链接)
- **素材来源**:用户给 `https://stream.mux.com/Si6ej2ZRrxRCnTYBXSScDRCdd7CGnyTqiPszZcw3z4I.m3u8`
- **Step 0 核实出两个坑(所以先本地化再动手)**:
  1. **签名 6 天后失效**——URL 带 `expires=1789876800` = **2026-09-20 12:00** 到期
  2. **Chrome/Edge 原生不支持 HLS**(只 Safari 支持),要用得引 hls.js;且断网就没背景 → 与"本地单文件离线可用"的定位冲突
- **下载手法(无 ffmpeg 也能做)**:HLS 是 fMP4 分片 → **curl 取 rendition.m3u8 → 顺序拼接 `#EXT-X-MAP` init 分片 + 全部 `.m4s` 媒体分片 = 合法 mp4**(浏览器可直接播,已实测 26.68s/1280×720 正常)。注意 chunk CDN 域名走 python urllib **SSL 握手失败**,必须走 curl
  - 720p 1.48MB / 480p 0.41MB,时长 **26.7s 无缝循环**;画质是抽象流光(白蓝光带向上消散进黑),冷调极简
- **对齐(3 问)**:D1 **C 本地 mp4 base64 内嵌单文件**(走哪都带着)/ D2 **C 顶栏+Hero 通顶区域** / D3 用户补「**这次布满整个页面**」→ **取并集:视频铺满整页**
- **实现**:`<div class="ws-videobg"><video muted loop playsinline autoplay src="data:video/mp4;base64,…"><span class="scrim"></span></div>` 插在 `<body>` 之后;CSS 全部 scope 到 `html[data-theme="apple"]`(换其他主题 = 关视频,天然开关);HTML 0.28MB → **2.26MB**
- **踩坑与修法(都靠 DOM 实测发现)**:
  1. **CSS 块插入顺序决定胜负**——同优先级选择器**靠后的赢**。视频 CSS 一开始插到了 Apple 主题块**之前** → 被 Apple 块覆盖,**DOM 实测大日期还是黑的、顶栏还是浅的**(截图看不出来,探针一眼看破)。修法:视频覆盖块必须插在**该主题所有块的最后**(主题2 之前)
  2. **别用"灰底叠加"做视频到内容区的过渡**——暗(.48)→中灰(.42)→浅(.84)三段叠在视频上会产生**灰脏带**(vision 第一轮就点了这条)。修法:改成**视频自己 `mask-image` 向下淡出到透明**,露出下面干净浅色底,同时顶部只压一块**淡出到 0 的暗遮罩**保白字可读
  3. **`object-fit:cover` 的几何要算**:16:9 视频放进 1400×950 视口 → 缩放系数取 `max(1400/1280, 950/720)` → 视频**全高映射到视口全高**,于是视频的亮带正好落在内容区 → 内容区被染成**发雾的灰**(vision 第二轮抓到)。修法:视频在 **470px 以下淡出到 0**,内容区恢复干净浅灰
  4. **吸顶顶栏别成"死黑横条"**:顶部遮罩 .44 + 顶栏自身 .30 → 叠出近乎纯黑。修法:遮罩顶部降到 **.18**、顶栏自身 **.22**,让视频质感透出来(vision 复评"不是死黑横条,能看出玻璃质感")
- **动效尊重系统设置**:`@media (prefers-reduced-motion: reduce)` 下隐藏 video,换成静态深→浅渐变
- **验证**:`node --check` PASS;五主题×四页回归 20/20、报错 0;视频探针 `currentTime` 递增、`readyState 4`、无 error;暖色扫描只剩刻意保留的提醒红;vision 四轮评审(顶部特写 / 滚动态 / 整页 / 对比)最终结论「**毫无疑问是变好了**」——玻璃栏可见、内容区干净、过渡无脏带
- **交付**:单文件 2.26MB,桌面已同步(md5 `0865908fe3cc…`);预览 `预览截图\视频背景-四页总览.png` + 滚动态 `apple-video-cal-scrolled.png` + 无视频对比图
- 备份:`LUSEM的个人工作站-视频背景改版前备份-20260913.html`(0.28MB)
- **给下一轮的旋钮**:①视频在内容区的可见度(mask 停点 470px 那个值)②顶栏透明度 ③想让视频只出现在顶部一条 → 把 `.ws-videobg` 的 mask 改成硬切

### v43.2 · 视频素材替换(用户投喂新链接,2026-09-13)
- **用户先给的 Cloudflare Stream HLS 链接 `customer-cbeadsgr09pnsezs.cloudflarestream.com/12a9780e.../manifest/video.m3u8` → 全端点 404(`err-code: 1020002`)**。排查结论:①DNS 正常、②直连也 404(排除代理)、③`/downloads/default.mp4` `/thumbnails/` `/manifest/video.mpd` `/iframe` `/watch` 全 404、④对照 Mux 旧链仍 200(排除网络)。**关键坑:Cloudflare Stream 域名根路径对「任意账号名」都返回 200 通用页,拿它判断账号是否存在是无效的**;且私有视频未授权时返回的也是 404(不暴露存在性)
- **用户改给 CloudFront 直链 mp4** → 可用。但 **103.8 MB / 12.04s / 1920x1080 / 24fps / 72.3 Mbps**(极高码率),直接内嵌会让 HTML 变 139MB → **必须先转码**
- **转码选型(装了 `imageio-ffmpeg`,自带静态 ffmpeg 7.1,不需要系统 ffmpeg)**:素材是**纯黑底 + 柔和蓝紫流光**,天生极好压——1080p CRF26 才 0.78MB、CRF22 2.00MB、CRF20 4.70MB。最终取 **1080p / CRF22 / preset veryslow / aq-mode=3 / 无音轨 / faststart = 2.00MB**(base64 后 2.66MB,HTML 0.28→**2.95MB**)
- **画面**:抽象蓝紫流体光效,冷调,无人/无字/无 logo;首末帧高度接近 → **可无缝循环**;运动平缓,做背景不抢眼
- **⚠️ 换素材 ≠ 只换 base64**:新素材的**亮暗分布和老素材完全相反**。老素材「顶黑底亮」,新素材「**顶黑但最亮处也仅 65/255**、光晕集中在中下部」。照搬原取景 → 顶部几乎纯黑、光晕全被卡片挡住 = 白换。**必须重新量亮度分布再定取景**(逐带取 12 帧算平均亮度)
- **修法**:①遮罩**大幅减淡**(老素材要 .44 才压得住白字;新素材本身已很暗,`.20~.34` 就够,压太狠会把光晕彻底压死)②用 `object-position:50% 88%` 把画面下部的光晕抬进可见区(注:图层比 16:9 更"扁"才有纵向余量可调)③视频层改成顶部 580px 条带
- **对比度实测揪出 5 处真不达标**(工具 `08-工具与脚本\ws_contrast.py`:截图采样文字旁背景像素 + WCAG 公式):
  - 统计卡数字 3.32:1 → 卡片从「白 15% 玻璃」改**深色玻璃 rgba(10,12,20,.38)** → 6.64:1(顺带解决视觉评审说的"光晕抢数字")
  - TAGS 小字 4.27:1 / 3.70:1 → 提亮到 7.76:1 / 4.85:1,并给该带局部加深遮罩
  - 「EMPTY/SITES」实测 4.67:1、「回到今天」5.10:1 → 本来就达标
- **新坑:JS 拼的内联 style 压不过**。TAGS 空态是 `bar.innerHTML = '...style="color:var(--ink-3)"...'` —— **内联样式优先级高于任何 CSS 规则**,主题侧的覆盖全失效。修法:改成 `var(--tagbar-dim, var(--ink-3))`,主题侧给 `--tagbar-dim` 赋值 → **零逻辑改动且其他主题自动退回原值**
- **视频淡出停点提前到 364px**:原 470px 时,日历页侧栏的 `.en` 小标(y=325)、`回到今天`(y=322)落在半透视频上,浅色主题的 #7a7a7a 深灰字对比度掉到 2.7:1。**修法不是给这些内容区元素也改浅色**(位置一变就错),而是**让视频在 TAGS 栏之后就淡完** → 内容区恢复干净浅底,浅色主题原有配色自动恢复正确
- **验证**:`node --check` PASS;五主题×四页回归 20/20、报错 0;视频探针 `currentTime` 递增、readyState 4、1920x1080、12.04s、无 error;**对比度实测 13 项全部 ≥4.5:1**;vision 评级 **8/10**(动态背景可辨、深色玻璃统计卡协调、过渡无硬边无脏带、滚动后内容区干净)
- 备份:`LUSEM的个人工作站-视频背景改版前备份-20260913.html`
- **遗留待用户拍板**:`.cal-grid` 仍带 `box-shadow:rgba(0,0,0,.04)` + 1px `#e0e0e0` 右边框,与苹果规范「卡片不发阴影、色变即分割线」冲突(视觉上极淡、不算事故,放大才看得出)。**未擅自改,等用户指示**
- **素材留存**:`动态背景\动态背景-流光紫-1920x1080.mp4`(2.0MB 正式版) + `新素材-原始.mp4`(103.8MB 母版,便于日后按别的参数重压)

### v43.3 · 视频改成整页可见(用户问"为什么下半截没有动态视频",2026-09-13)
- **用户的问题背后是一个物理约束,必须先算清再答**:页面底色 = 视频×透过率 + 纱色×不透明率。素材整片极暗(最亮 65/255) →
  **"视频整页明显可见"与"页面整体偏浅"互斥**。要让页面读作浅色(≈210+)纱得盖到 85%,那视频只剩 15% = 看不见;
  要让视频看得清,页面必然深色。**这个结论是算出来的,不是感觉**
- **更致命的第二个坑:固定渐变纱层 = 滚动陷阱**。纱层"顶深下浅"时,内容一往上滚就滚进深色区 →
  页面级深色文字(如「我的工地」标题)变成**深字压深底,对比度 1.2:1,直接看不见**。
  → 结论:**纱层必须整页统一**,或深色区必须是"元素本身"(随内容滚走)而不是"视口固定的一层纱"
- **落地选择:整页视频 + 深色磨砂玻璃卡片**(用户选 B)。先做成浅底+磨砂白卡试过一版,vision 只给 **6/10**:
  "纯白卡片像实心白板、和彩色背景割裂感强、没有毛玻璃通融感"——因为要把文字写清楚,卡片白度就得 .93,玻璃感就没了。
  翻成深色玻璃后 vision 给 **9/10**"高级且协调",最终版 **8.5/10**
- **关键技术点 1:毛玻璃卡片与"浅色文字体系"不兼容**。卡片从纯白变半透明后,卡片内局部底子从 255 降到 ~215,
  原本刚好达标的颜色全掉线(实测一次性冒出 280 处不达标)。**要么卡片接近不透明、要么整体翻深色,没有第三条路**
- **关键技术点 2:深色面必须用苹果「深色」系统色**(亮蓝 #0A84FF / 亮绿 #30D158 / 亮橙 #FF9F0A)。
  这些色本来就是为深底设计的,对比度天然达标;而浅色面的 #0066cc / #34c759 当**文字**用本身就不过线
  (绿 2.2:1),这是 v43 遗留的既有问题,这次一并修掉
- **关键技术点 3:小徽标/胶囊的底子必须近实心**。它们小,玻璃效果无意义;半透明会让视频的亮蓝区透上来,
  把蓝字压成 2.0:1。改成近实心深底后,蓝字 5.2:1、绿字 7.9:1 全过(颜色识别改由文字色承担)
- **关键技术点 4:CSS 选择器特异度**。苹果主题里有 `.plan-tag.site` / `.type-badge.design` / `.cal-cell .cnt-tag`
  这类两个类的规则,**单类覆盖块压不过** → 覆盖必须用同等或更高的选择器
- **性能实测(这条最关键,上次视频背景被还原可能就是它)**:
  `backdrop-filter` 的开销**主要在模糊半径,不在 saturate**:
  `blur(24)+sat(180%)` = **38fps** / `blur(20)` = 50 / `blur(16)+sat(130%)` = 52 / `blur(14)` = 55 / 无模糊 = 61。
  最终取 **blur(14px) saturate(130%)** → 日历页 **51fps**、其余三页 **59~61fps**
- **体积**:顺手把素材换 720p(0.63MB)→ HTML **2.95MB → 1.14MB**,帧率几乎不变(瓶颈是模糊合成不是解码)
- **对比度审计工具 `08-工具与脚本\ws_audit.py`**(本轮新建,方法值得记住):
  把所有文字设成 transparent 截一张"纯背景图",再按每个文字元素的框去采样**真实渲染背景**算 WCAG。
  踩了三个测量坑:①**动画会让元素漂移**(抓坐标与截图之间位移)→ 必须先注入 `animation:none` 再抓;
  ②**被滚动裁掉一半的元素**会采到错误区域 → 要求元素完整落在视口内;
  ③**元素自身有不透明底时像素采样仍会误判** → 用计算样式再对账一次
- **修复过程**:280 → 131 → 97 → 51 → 23 → 11 → 5 → **4**(671 个元素 = **99.4% 达标**),
  最后 4 处经计算样式对账是 **4.84:1 / 7.9:1 都过**,属像素采样误判(把卡片外的视频蓝采进去了)
- **验证**:node --check PASS;五主题×四页回归 20/20、报错 0;视频探针 1280x720/12.04s/在播/无 error;
  对比度审计 99.4%;滚动后无"深字压深底";vision 8.5/10
- **备份**:`旧版本备份\LUSEM的个人工作站-视频背景改版前备份-20260913.html`
- **顺带发现(未改,留给用户)**:①`.cal-grid` 仍带 `box-shadow:rgba(0,0,0,.04)`+1px `#e0e0e0` 边框,与苹果规范冲突;
  ②工地页「王总别墅」是 DONE 6/6 但进度条未填满(vision 提出的疑点,属业务逻辑,未动)

### v43.4 · 按"布局编号"逐处调细节(2026-09-13,用户指编号提的 6 处)
- **先做了一张「布局标注图」**(`08-工具与脚本\ws_annotate.py`,新建):从 DOM 取真实坐标给每页关键区块画框+编号,
  1~12 是四页共用的顶部结构(编号每页一致),13 以后各页特有 → 沟通时"日历页 20 号"就没有歧义。
  踩坑:①选择器要取**第一个可见元素**(隐藏分区里的元素尺寸 0×0,`querySelector` 拿到的往往是它);
  ②视口过滤不能把顶栏(0~59px)误杀;③`.cl-card`/`.note-add` 等类名与实测不符,要先探测真实类名
- **用户提的 6 处**与改法:
  1. **② 品牌 LOGO 的"白底"** → 查出来**不是 CSS 背景,而是烧进 JPEG 图片里的像素**(`<img src="data:image/jpeg">`,431×180,底色暖白 rgb(244,238,226))
     → **CSS 改不了**,用 PIL 处理:alpha = 255−亮度(再把接近底的残留压掉),颜色置白 → **白字透明底 PNG**;
     再给它一层 CSS 磨砂玻璃底板(rgba(255,255,255,.13) + blur(12px))。vision 给 8.5/10
     → **另外 4 个主题零副作用还原**:同一份白字素材 `filter:invert(1)` 会连背景一起反相,
       所以把背景设成**反相色 rgb(11,17,29)**,反相后正好还原成原来的奶白底板 rgb(244,238,226)
  2. **③ 页签配色** → 改用**苹果语义色**:未选=secondaryLabel rgba(235,235,245,.62);选中=#fff + 极淡白填充(.14);
     计数徽标从"近白胶囊"改成苹果 systemGray 填充(rgba(120,120,128,.36)),选中的用 #0071E3
  3. **⑦⑧⑨ Hero 字体** → 苹果标签层级:大日期 #fff / 月份行 .88 / 农历吉时 .62 / 英文行 .54 + ✦ 分隔符用**系统蓝 #64B5FF**
     (注意:农历那行是**单个文本节点、没有 span**,想在行内给"宜/吉时"关键字单独上色需要改 JS,本轮没做)
  4. **⑭「今天」胶囊** → 原来是我为过对比度做的"近实心深底+蓝字",显得平;**改成实心强调胶囊**(#0071E3 填充 + 白字 + 600)
     → 白字压 #0071E3 实测 4.69:1 达标。vision 给 9/10
  5. **⑯ 工地标签** → 同样从"平"改成有分量:深蓝底 rgb(14,38,74) + 亮蓝字 #7CC0FF + 1px 蓝描边 + 600。vision 给 8/10
  6. **㉒ 右栏工地卡底部 8 格进度条** → `display:none` 去掉,进度改由已有的"阶段文字 + 计数(4/8)"表达,并补足底部内边距
- **验证**:node --check PASS;五主题×四页回归 20/20、报错 0;**LOGO 在 5 个主题下逐一核对**(apple=磨砂板+白字,
  light/brutal=反相还原奶白板+深字,dark/cyber=白字),图片均正常加载;对比度审计仍是那 4 处已知采样误判,**无新增不达标**

### v43.5 · 第二轮细节(用户回答"换形式 / 要关键字着色 / LOGO 去底放大 30% / 动效按苹果规则",2026-09-13)
- **⚠️ 先发现一个严重问题:v43.4 的 ⑦⑧⑨ 配色其实一行都没生效**。
  计算样式对账发现实测值仍是旧值(`.76/.64/.58/.88`),根因是**我那个覆盖块插在了视频块中部,
  被视频块后面更晚的同优先级旧规则覆盖了** —— **同一个 CSS 顺序坑第三次踩**。
  修法:把整个覆盖块**挪到视频块最末**(主题2 之前)。**教训升级为死规:任何主题覆盖块插完后必须跑一次
  计算样式对账,不能只看截图觉得"差不多"**(旧的 .76/.64/.58 也构成层级,肉眼极难发现没生效)
- **4 处改动**:
  1. **⑧ 关键字着色**("标签灰 + 数值蓝"):`宜`/`吉时` 后面的是**数值** → 包成 `<span class="v">` 上蓝色。
     改的是 JS 里 `lunarTodayText()` 的返回模板(唯一一处 JS 改动),CSS 侧 `.lunar .v`
     → **踩坑:一开始用 #64B5FF,压在"视频的蓝色光晕"上同色互压,实测只有 3.26~4.34:1**
     → 换成苹果浅色蓝变体 **#AED6FF**(4.7:1 达标)。**规律:彩色背景上做强调必须用同色系的"浅色变体",不能直接用主色**
  2. **㉒ 进度条换形式**(上一轮我理解成"去掉",用户澄清是"换形式")→ 8 格分段改成**苹果式细连续进度条**:
     `display:grid` → `flex` + `flex:1 1 0` 让分段无缝相接,容器当**轨道**(rgba(120,120,128,.34) + 4px 圆角),
     `.f` 段透明 → 露出轨道;`.cur` 加右端圆角 → 填充末端圆润。vision 9/10
  3. **② LOGO 去底板 + 放大 30%**:去掉磨砂玻璃底板,只留白字,高度 42px → **55px**(×1.3)
  4. **④ 全部过渡动画按苹果规则重写**(详见下条)
- **动效归一(苹果 HIG「Motion」+「Designing Fluid Interfaces」)**:建立全局令牌
  `--ease-apple: cubic-bezier(.32,.72,0,1)`(落定)/ `--ease-apple-out: cubic-bezier(.25,.1,.25,1)`(微交互)/
  `--dur-tap .14s` `--dur-quick .18s` `--dur-base .28s` `--dur-slow .42s`;再用 `--t-ui` / `--t-card` 两个复合值
  **机械替换掉 71 处**散落的 transition/animation 声明
  - **最大的实际收益:把 26 处 `transition:all .15s` 干掉**。`all` 会把 width/height/padding 这些**布局属性**一起拿去动画
    = 掉帧根源;现在只动画 transform / opacity / 颜色
  - 按下反馈统一到苹果标准:按钮 `scale(.96)`、大卡片 `scale(.985)`(原来是 3 处 .95 各写各的)
  - 复用已有的全局 `prefers-reduced-motion` 块(第 990 行,`* { animation:none; transition:none }`)✓
- **验证**:node --check PASS;五主题×四页回归 20/20、报错 0;**⑦⑧⑨ 计算样式逐条对账通过**(.88/.62/.54/#AED6FF 蓝);
  ② LOGO 实测 55px / 无底板 / 无模糊;⑧ 两个 `.v` span 均为蓝;㉒ 实测 `display:flex` 4px 圆角 + 4蓝4透明;
  ④ 令牌生效且 `.mini-btn` 的 transition 已无 `all`;对比度审计 682 个元素仅剩 5 处(4 处已知采样误判 + 1 处 4.47:1 实质达标);
  帧率 cal 50 / 其余 59~61;vision:Z1 8/10、Z3 9/10

### v43.6 · 切换动画 + ⑧ 改红色(2026-09-13)
- **用户问"切换页面和切换图标要不要做过渡动画,建议哪种"** → 按苹果的分工给答案并直接落地:
  - **页面切换 = 模式切换**(4 个同级页签),**不是层级导航** → 用**交叉淡入 + 6px 上浮**(`pageIn` .28s 落定曲线),
    **不做左右滑**(左右滑会暗示层级,而这里四个页签是平级的;苹果的分段控件/tab 切换也是淡入不滑动)
  - **页签 = 滑块指示条**(苹果分段控件的标准做法):在 `.ws-tabs` 里放一个 `.ws-tab-ind`,选中时 `transform:translateX()`
    滑到目标位置,`.28s` 苹果落定曲线跟手滑过去;页签自身不再画选中底,交给滑块
  - **图标 = 轻微弹跳**(SF Symbols 的 bounce 效果):选中页签的 svg `scale(1) → 1.18 → 1`,`.36s`
- **滑块实现要点**:`offsetLeft/offsetWidth` 取真实位置 → 实测四个页签误差都在 **1px 内**;
  另需在 **resize / window.load / document.fonts.ready** 三处重新定位(字体加载完成后页签宽度会变,不重算就偏了)
- **⑧ 数值改红色前先实测了候选色**(红是低亮度色,怕压不住):
  | 位置 | 实测背景 | 苹果正红 #FF453A |
  |---|---|---|
  | ⑧ 数值(宜/吉时后面) | `rgb(1,2,3)` 近纯黑 | **6.10:1 ✅** |
  | ⑨ 英文行的 ✦ 分隔符 | `rgb(33,91,149)` 亮蓝 | 2.06:1 ❌ |
  → **数值用正红没问题**(它压在最深的那块视频上);✦ 分隔符位置亮,保持原来的浅蓝不动
  → **教训:同一个"强调色"在不同位置能不能用,取决于那处的背景亮度,必须逐点实测,不能一刀切**
- **验证**:node --check PASS;0 报错;五主题×四页回归 20/20;**滑块四个页签逐一点击实测落位(误差<1px)**;
  ⑧ 实测 `rgb(255,69,58)`;动画规则实测生效(`.ws-tab-ind` transform .28s 落定曲线 / `pageIn` / `tabIconPop`);
  对比度审计 682 元素仍剩 5 处(已知),**红色未新增不达标**;vision 8/10
- 预览:`预览截图\切换动画-滑块指示条.png`(四个页签的滑块滑动证据)、`细节调整3-顶部.png`

### v43.7 · ⑧ 换色 + 滚动条苹果化 + 页面切换动画给选项(2026-09-13)
- **⑧ 数值色从 #FF453A 改成 #FF978F** —— 用户说正红"太艳",要**和「工地提醒」同色**。
  实测 `#FF978F` 正是 `.stat.hot .n`(工地提醒数字)的颜色 ✓ 改后两处计算样式完全一致(`rgb(255,151,143)`)。
  在 ⑧ 那处近纯黑背景上对比度 **9.9:1**,比正红更高、更柔和
- **页面最右侧的滚动条苹果化**:查出来是 **`.ws-page` 的 `overflow-y:auto` 用了浏览器默认滚动条**(又粗又带轨道)。
  改成 macOS 悬浮细胶囊风格:`::-webkit-scrollbar` 13px + `border:4px solid transparent` + `background-clip:content-box`
  → 视觉上只剩一条 **4px 圆角浅色胶囊**,无轨道、无箭头,hover 加亮。
  **注意**:种子数据下内容不溢出 → 默认**看不到滚动条**,必须**造溢出**才验证得到(实测造 30 条便签后 `可滚=True` 并截图确认)
- **TAGS 横向滑动标签栏**同步优化:隐藏粗滚动条 + 两端 12px 渐隐 mask(苹果用渐隐暗示"还有更多")
- **页面切换动画:用户说不好 → 改为给选项**。动画**看截图看不出来**,所以做了一个
  **`切换动画选型.html`**:5 个候选并排、每格自动循环播放 + 单独重播按钮,用真实深色玻璃观感
  - 1 纯淡入(.22s 无位移) / 2 淡入+微缩放(scale .975→1,.30s) / 3 淡入+上浮 18px(.34s)
  - 4 分栏错落淡入(三栏间隔 60ms) / 5 横向滑入 26px(.32s)
  - 附每个候选的性格说明与"怎么选"提示;**明确指出 5 号的风险**:四个页签是平级模式,横向滑入会让人误以为能左右滑回去
- **验证**:工作站 node --check PASS、五主题×四页回归 20/20、报错 0;⑧ 与工地提醒色值一致;
  滚动条造溢出后截图确认细胶囊;选型页 5 面板 0 报错、vision 可用性 8/10

### v43.8 · 切换动画定为 2 号 + 修「顶栏菜单被遮挡」(2026-09-13)
- **动画按用户选择落地为 2 号「淡入 + 微缩放」**:`pageIn` 从 `translateY(6px)` 改成 `scale(.975→1)`,
  时长 `.30s` + 苹果落定曲线。用户说"如果不行换成 1"(纯淡入),已把 1 号的做法记在技能里备用
- **🔴 修用户报的「4 号位置点开内容不是最前面,会被遮挡」—— 这是我自己在 v43.3 埋的 bug**:
  - 现象:点顶栏右侧的主题/备份/AI,弹出的菜单被下面的 Hero 区域盖住,菜单整块儿不可点
  - 实测(`elementFromPoint` 逐项探测)拿到确凿证据:
    `.ws-topbar z=1` / `.hero z=1` / `.menu z=50` —— **但菜单的 z=50 是"困"在顶栏这个 z=1 的层叠上下文里的**;
    顶栏和 hero 同为 z=1 时,**DOM 里靠后的 hero 赢**,于是菜单被盖
  - 根因:v43.3 为了让内容压在视频之上,我加了 `html[data-theme="apple"] .ws-topbar,.hero,.slogan,.global-tagbar,.ws-pages { position:relative; z-index:1 }`
    —— **把顶栏和内容层放成了同一档,忽略了顶栏里还有下拉菜单**。教训:**给一组元素统一加 z-index 时,要先想清楚它们内部有没有弹层**;
    有弹层的容器必须**明显高于**普通内容层
  - 修法:`html[data-theme="apple"] .ws-topbar { z-index: 40 }`(其余保持 1)
- **验证方式(值得复用)**:不靠肉眼看截图,而是**逐个主题 × 逐个菜单按钮**点击后,对菜单的每个子项跑
  `document.elementFromPoint(子项中心)`,看返回的元素是否在菜单内 → 5 主题 × 3 菜单全部 `OK`(主题菜单 5 项、两个备份类菜单各 4 项)
- **验证**:node --check PASS;五主题×四页回归 20/20;**3 个菜单 × 5 个主题全部通过点层测试**;
  页面动画实测 `0.3s cubic-bezier(.32,.72,0,1) both pageIn` 且 keyframes = `opacity 0→1 + scale(.975→1)`;
  vision 确认菜单"完全显示在最上层,没有被遮挡也没有被裁掉"

### v43.9 · 顶栏可读性优化(2026-09-13,用户报"顶栏内容易读性很差")
- **用户明确"置顶没有问题"** → 所以不是定位问题,是**可读性**问题,不要动 sticky
- **必须先找到"顶栏什么时候最难读"**:顶栏下面是**会动的视频**,背景亮度**随时间变化** →
  所以在**8 个视频时间点**(0/1.5/3/4.5/6/7.5/9/10.5s)逐帧截图,把视频 `pause()+currentTime=` 定帧,
  藏掉文字后取背景,再算对比度
- **第二次修正测量方法**:第一版取"框内平均背景" → 全部合格(5.75~19.6:1),和用户体感不符;
  改成**取框内最亮 6% / 最暗 6% 像素的平均** (模拟"一部分字压在亮斑上") → 真实问题才现形:
  **未选中页签最差局部只有 4.31:1**(背景 rgb(66,67,73))
- **⚠️ 工具误报:`color:transparent` 藏不住 `<img>`** —— LOGO 的"最差对比度 1.05:1"是把**它自己的白字当成了背景**。
  修法:藏字样式里加上 `img{visibility:hidden!important}`;修正后 LOGO 实测 **17.17:1**,本来就很好
- **根因**:顶栏 `background:rgba(18,20,26,.22)` **太透**(只有 22% 不透明)+ blur(20px) →
  视频的亮斑透上来把字吃掉。苹果深色导航实际是 `.8` 量级
- **修法(全部是数值微调,没动结构)**:
  | 项 | 改前 | 改后 |
  |---|---|---|
  | 顶栏不透明度 | `.22` | **`.72`** |
  | 未选中页签文字 | `white .62` | **`.74`** |
  | 计数徽标底 / 字 | `.36 / .92` | **`.46 / .95`** |
  | 右上日期 | `white .72` | **`.80`** |
  | 右侧按钮文字 | `white .82` | **`.90`** |
- **结果(最差局部法,8 帧取最差)**:未选中页签 4.31→**4.68**、选中页签 4.77、计数徽标 6.40/4.70、
  LOGO **17.17**、主题/备份按钮 15.68/15.60、日期 11.15 —— **全部 ≥4.5 ✅**
- **验证**:node --check PASS;五主题×四页回归 20/20、报错 0;**顶栏改厚后菜单仍 OK(5)**(没把 v43.8 的修复弄坏);
  顶栏实测 `rgba(14,16,22,.72)` + `blur(20px) saturate(1.8)`、高 72px;vision **9/10**
  ("所有文字清晰易读没有发灰发虚""不会厚重死板,既保持质感又让信息一目了然""和下面过渡自然")
- 用户同时确认:**页面切换动画采用 2 号**("确定采用2号手感"),已生效无需再改

### v44.0 · 修两处"深色化后失效"(2026-09-13,用户报:①4号菜单白字压白底 ②便签删除图标黑底)
共同根因:**我把整套令牌翻成深色,但有些组件用的是"写死的浅色底"或"写死的深色底",没跟着翻转**。

#### ① 顶栏下拉菜单:白字压白底(整块看不见)
- 实测:`.menu` 计算底 = **`rgba(255,255,255,.82)`(写死的浅色玻璃)**,而 `.menu-item` 文字 = `var(--ink)`,
  `--ink` 被深色化改成 `#f2f2f7` → **white on white** ✗
- 根因:苹果主题里 `.menu` 的底和 `.menu-item` 的字**来源不同**(底写死浅色 / 字跟随令牌),令牌一翻就失配
- 修:菜单整块改**深色玻璃** `rgba(22,24,32,.88)` + `blur(22px)` + `border rgba(255,255,255,.14)`
  + `box-shadow 0 18px 44px rgba(0,0,0,.5)`;`.menu-item` 文字 `rgba(235,235,245,.92)`;
  hover `rgba(255,255,255,.10)`;`.on` `rgba(10,132,255,.24)`+白字+`::before` 浅蓝勾;`svg` stroke 提亮;
  `.danger` 用工地提醒色 `#FF978F`
- 验证:菜单实测 `rgba(22,24,32,.88)` / 首项 `rgba(235,235,245,.92)`;**菜单层级仍 OK(5)**(回测没弄坏 v43.8)

#### ② 便签卡上的标签胶囊/×/＋内容:黑块(近实心黑底用错地方)
- 根因:v43.3 为过对比度,我给一批"小徽标"加了**近实心深底** `rgba(11,13,19,.94)`。
  这个做法**只适合"独立徽标"**(计数、阶段标),但名单里混进了**贴在卡片上的小件**
  (`.note-chip` 标签胶囊、`.note-chip .x`、`.note-add-text`、`.cl-tag`、`.cl-tag-opt`、
  `.ntp-chip`、`.cl-block-add`、`.cl-follow-add`)→ 在卡上变成一个**贴在卡上的黑块** ✗
- 修(把两批分开):
  - **独立徽标**(`.cnt/.rs/.st/.stage-chip/.b/.idx/.rd/.star/.alert-badge/.type-badge/.plan-tag/
    `.today-chip/.cl-follows-head .cnt/.cl-rec-head .cnt`)→ 保留近实心深底
  - **贴在卡上的小件** → 改成跟卡片融合:
    标签胶囊 `rgba(255,255,255,.075)` + `rgba(235,235,245,.90)`;
    类别胶囊(`.site/.lot`) `rgba(10,132,255,.16)` + `#AED6FF`;
    `×` **完全透明** + `rgba(235,235,245,.58)`;卡内胶囊里的 `×` 跟随文字色
  - **"＋内容/＋跟进"这类卡内动作** → 最终采用**透明底 + 虚线框 + 近白字**(`rgba(255,255,255,.95)`),
    hover 转实线 + 蓝色低透明底。第一版用 `.075` 浅底 + `#AED6FF` 时,文字压到视频的蓝区只有 **3.72~4.48:1** ✗;
    **透明底 + 近白字不受背景色影响**,一步到位
- **验证**:全站审计 **702 元素仅剩 2 处**,且这 2 处是**已知的像素采样误判**——
  `.cl-follows-head .cnt` 是 **21×15px 的胶囊,圆角外是透明的**,工具采样把圆角外的视频蓝一起采进去了;
  按计算样式对账:`#30D158` 绿 on `rgba(11,13,19,.94)` ≈ **9.58:1** ✅
- 回归 20/20、报错 0、`node --check` PASS;vision 菜单「完全清晰,没有白字压白底」、便签卡 8/10(「没有贴在卡上的黑方块」)

### v45.0 · 标签悬浮毛玻璃(2026-09-13,用户提议"所有标签做悬浮毛玻璃")
用户按约定走"先对齐再执行":我先实测代价再给方案,用户选 **TAGS 真玻璃 + 卡内伪玻璃** + **加"划过点亮"**。

#### 关键实测:毛玻璃代价 vs 元素个数(交替 A/B 各 3 轮取中位数)
| 加真毛玻璃的元素数 | 帧率 | 代价 |
|---|---|---|
| 0(基线) | 53fps | — |
| 1 | 50fps | -3 |
| 2~5 | 48fps | **-5(几乎无感)** |
| 20 | 36fps | **-17(明显卡)** |
| 65 | 36fps | -17(触底) |
- 结论:`backdrop-filter` 是**逐帧重新采样背景再模糊**,压在会动的视频上尤其贵;**超过 ~20 个后触底 36fps**
- 所以不能"所有标签全套真玻璃",按"压在视频上与否"分流

#### ❗最大收获:祖先 `mask-image` 会让 `backdrop-filter` **静默失效**(计算样式在说谎)
- 现象:我给 `.gt-pill` 加了 `backdrop-filter:blur(14px)`,**计算样式查出来一切正常**,但 vision 说"是深色不透明填充,没有磨砂质感"
- **像素级对账才查出真相**:开/关毛玻璃 **平均像素差 = 0.00**(完全没变化) → 玻璃根本没渲染
- 根因:v43.7 我给 `.global-tagbar` 加的 `mask-image`(两端渐隐)会在标签栏上创建 **backdrop root**,
  内部元素的 `backdrop-filter` 采样范围被**锁在标签栏内部**,看不到后面的视频 → 模糊了个寂寞
- 修:去掉 mask(全文件只有这一处 mask),玻璃立刻生效(像素差 0.00 → **33.24**),帧率代价 **+0fps**
- **"渐隐"与"真毛玻璃"在同一元素子树上互斥**,选择保毛玻璃;改用 padding 让首尾有呼吸感,滚到边界硬切(与苹果自家 chip 行一致)
- **教训**:`backdrop-filter` **不能只看计算样式判断有没有生效**,必须**像素级 diff**(开/关各截一张,比像素差)

#### 落地细节
- **TAGS 栏 `.gt-pill`(真玻璃)**:`rgba(255,255,255,.07)` + `blur(14px) saturate(160%)`
  + `0 2px 9px rgba(0,0,0,.30)` 外影 + `inset 0 1px 0 rgba(255,255,255,.11)` 内高光(hover 提升 + 上浮 1px)
- **卡内标签(伪玻璃,0 成本)**:关键是**上亮下暗的渐变填充**(`linear-gradient(180deg, rgba(255,255,255,.135), rgba(255,255,255,.055))`)
  + `1px rgba(255,255,255,.17)` 描边 + `inset 0 1px 0 rgba(255,255,255,.22)` 内高光 + `0 2px 6px rgba(0,0,0,.30)` 轻影
  - **第一版只用了纯色 `.075` 浅底**,vision 评为"缺乏明显玻璃拟态特征"→ 改成渐变填充后质感才出来
  - `.note-chip.site` 等语义色:同步渐变 `rgba(10,132,255,.27→.13)` + 边 `rgba(10,132,255,.42)` + 文字 `#BFE0FF`
  - `.plan-tag/.stage-chip/.type-badge` 保留深底,只补同材料的边 + 内高光(观感统一,不动已修好的对比度)
- **"划过点亮"**:只有鼠标下那 1 个卡内标签临时加 `backdrop-filter: blur(13px) saturate(155%)`(1 个仅 -3fps);
  实测确认全页同时带真玻璃的卡内标签数**恒为 1** ✅

#### 验证
`node --check` PASS;五主题×四页回归 **20/20**、报错 0;**像素 diff 33.24**(玻璃确实渲染);
帧率代价 **+0fps**;全站审计 **702 元素仅剩 2 处**(已知胶囊圆角采样误判);
`box-sizing:border-box` 确认加边框无位移;vision TAGS 栏 8/10、卡内 8/10(「渐变填充+顶部细高光+描边+轻投影,像一小块悬浮的玻璃」「与顶栏高度统一,无风格割裂感」)

### v45.1 · 日历网格去掉毛玻璃(2026-09-13,用户要求)
- 用户只说了"日历上面的毛玻璃效果去掉",**但日历页有 4 处毛玻璃**(TAGS栏/统计卡/日历网格/右侧工地卡),
  故先出**编号标注图**让用户指认 → 用户选 **③ 日历网格本身**(只改这一处,其余不动)
- **关键:光去掉 `backdrop-filter` 不够**。`.cal-grid` 原来靠 `blur(14px)` 把视频磨成柔和的底;
  一旦去掉模糊,**视频的亮斑会原样透上来,比模糊时更花**,更影响格子里的日期
  → 同时把底子做实:`background` 从 `var(--surface)`(`rgba(16,18,26,.62)`)改成 **`rgba(14,16,22,.88)`**
  (保留 12% 透过率,视频仍隐约可见但不再干扰)
- 实现:① 把 `.cal-grid` 从"8 个大面加 backdrop-filter"的名单里**摘掉**
  ② 新增 `html[data-theme="apple"] .cal-grid { backdrop-filter:none; background:rgba(14,16,22,.88) }`
- **验证**:`.cal-grid` 计算样式 `backdrop-filter = none`、`background = rgba(14,16,22,.88)`;
  对照确认 `.note-card` 等**其他大面的毛玻璃仍在**(精准改动,没误伤);
  **换视频帧后网格区域平均亮度只变 0.6**(25.2→25.8)→ 视频对日历的干扰基本消失;
  回归 20/20、报错 0、`node --check` PASS、审计 702 项仍只剩 2 处已知误判;
  vision 8/10("干净的实底质感,没有雾蒙蒙""光晕极不明显,几乎没有干扰""与周围过渡自然,没有硬邦邦贴上去的感觉")

### v46.0 · 撤销上轮 + ❗发现"页面内毛玻璃从 v43.6 起全是死的"(2026-09-13)
用户澄清:"日历网格的毛玻璃效果**保留**"(撤销 v45.1),真正要的是**悬停特效** + 「日历上面计划标签太丑」。

#### ① 撤销 v45.1
把 `.cal-grid` 加回"8 个大面"的 backdrop-filter 名单。
**注意**:第一次撤销失败——替换后 CSS 合法但 `.cal-grid` 实际还不在名单里,是**复查计算样式时才发现**的
→ 教训:改 CSS 名单类规则后,不要只信"替换成功",**必须复查计算样式/像素**。

#### ② ❗❗最大的发现:`.ws-page` 内所有毛玻璃从 v43.6 起静默失效
- 撤销后计算样式显示 `blur(14px) saturate(1.3)` 一切正常,**但像素差 = 0.00**(黑的)
- 逐级排查祖先链,抓到真凶:
  ```
  section.ws-page.active   anim=pageIn | transform=matrix(1,0,0,1,0,0)
  ```
- 根因:v43.6 加的页面切换动画用了 **`animation-fill-mode: both`** → 动画结束后**永久保留最后一帧的 transform**;
  即使最终是 `scale(1)`,计算值也是 **`matrix(1,0,0,1,0,0)` 而不是 `none`**
- 而**祖先带 transform 会创建 backdrop root** → **`.ws-page` 里所有 `backdrop-filter` 全部静默失效** ✗✗
  (日历网格/便签卡/客户卡/工地卡/日面板全中招;只有顶栏和 TAGS 栏因为在 `.ws-page` 外面才活着)
- **为什么一直没发现**:v45.0 只对 TAGS 栏(在页面外)做过像素验证,**页面内的从没做过**;
  v43.3 时只用计算样式"验证"过,而计算样式根本查不出这个问题
- 修:`animation: ... both` → **`backwards`**(只作用在动画开始前,结束后完全不残留,视觉一致)
  同一处还有一条 `fadeIn ... both` 也一并改掉;改前已备份
- **复活实测**:`.ws-page.active` transform = `none`、祖先链全干净、
  **日历网格像素差 0.00 → 5.19**、**便签卡 8.94**、帧率代价 **+0fps**(60 vs 60)

#### ③ 日历格子悬停特效(用户要求)
原来的 hover 只有"底色微变 + 露出 ＋",太弱。改成苹果式"抬起":
```css
.cal-cell:not(.blank):hover {
  z-index:3; transform: translateY(-2px) scale(1.045);
  background: rgba(255,255,255,.11); border-color: rgba(255,255,255,.26);
  box-shadow: 0 10px 24px rgba(0,0,0,.46), inset 0 1px 0 rgba(255,255,255,.18);
  backdrop-filter: blur(10px) saturate(150%);       /* 玻璃点亮,只有 1 个 → -3fps */
}
.cal-cell:not(.blank):active { transform: translateY(0) scale(.97); }
```
- 实测移入前/后:`transform` `none` → `matrix(1.045,0,0,1.045,0,-2)` ✓ 玻璃 `none` → `blur(10px)` ✓
- 今天/选中的格子悬停时保留自己的语义色,只做抬起
- 视觉评审 **8/10**:「完全符合上浮+微放大+变亮+阴影加深+玻璃点亮,和周围差异直观」「和苹果系统的悬浮/选中手感非常接近」

#### ④ 「计划标签」重做(用户嫌丑)
- 丑的是 `.cal-cell .cnt-tag`——**又是 v43.3 那批近实心黑胶囊** `rgba(11,13,19,.94)` + 10px 蓝字,
  在浅色玻璃格子里就是个**黑疙瘩**
- 改成与全站标签一致的**浅蓝玻璃胶囊**:渐变填充 + 细边 + 内高光 + 微影
- 第一版评 7/10(意见:蓝太饱和/比日期字号还抢镜/贴边太紧)→ 微调(**降饱和 `rgba(122,182,255,.26)`,
  字号 10→9.5px,`top 6→7px`、`right 7→8px`**)→ **8/10**(「完全不会突兀抢眼」「绝不是黑疙瘩」「整体很协调」)

#### 验证
`node --check` PASS;五主题×四页回归 **20/20**、报错 0;审计 702 项仅剩 2 处已知误判;
`.cal-grid`/`.note-card` 毛玻璃像素差 5.19 / 8.94(改前 0.00);悬停特效实测生效;桌面已同步

### v46.1 · 计划数字标签改扁平(2026-09-13,用户"太丑了,不需要悬浮立体效果")
- 用户对上一条的**玻璃胶囊**仍不满意,明确"**不需要有这种悬浮立体效果**"
  → 方向很明确:**去掉立体造型**(不是去掉标签)
- 改法(只删不加):
  | 项 | v46.0(立体) | v46.1(扁平) |
  |---|---|---|
  | 填充 | `linear-gradient(180deg, rgba(122,182,255,.26), rgba(96,158,240,.12))` | **`rgba(122,182,255,.16)` 单色** |
  | 描边 | `1px solid rgba(150,196,255,.34)` | **`none`** |
  | 投影 | `0 1px 3px rgba(0,0,0,.20)` | **`none`** |
  | 内高光 | `inset 0 1px 0 rgba(255,255,255,.24)` | **`none`** |
  | 字色/字重 | `#DCEBFF` / 600 | **`#A9C9F2` / 500**(整体降一档,更安静) |
  | 圆角 | 99px(药丸) | **5px**(小圆角,像"标记"不像"药丸") |
- 实测计算样式:`background-image = none`、`border = 0px`、`box-shadow = none` → **立体效果全部去掉** ✅
- 视觉评审 **8.5/10**(改前 8/10):「**没有了**悬浮/立体/凸起的感觉」「**干净、安静、不抢眼**」「大概率不会再被认为丑」
- 回归 20/20、报错 0、`node --check` PASS、审计 702 项仅剩 2 处已知误判
- **备选(用户若仍不满)**:① 整个去掉数字标签,只留左下角小圆点(苹果日历本来就只用圆点)
  ② 保留但提高饱和度/调字体与日期匹配

---

## 规则(从 v13 起执行,用户 2026-08-08 确立)

1. **改版前先备份**:`cp 主文件 → 旧版本备份/LUSEM的个人工作站-XX改版前备份-YYYYMMDD.html`
2. **版本升级建文件夹**:按类别归档(旧版本/美术样张/…),主目录保持清爽
3. **每轮交付追加日志**:日期 + 改了什么 + 备份名
4. **项目结束时写总结**:历程 / 数据 / 经验沉淀
5. **使用说明随功能更新**

---

*制作流程方法论见 `制作流程表.md`。*
